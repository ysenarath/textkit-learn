{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"api/","title":"API Reference","text":"<p>Welcome to the TextKit-Learn API Reference. This documentation is automatically generated from the source code docstrings.</p>"},{"location":"api/#modules","title":"Modules","text":"<ul> <li>Embeddings - Text embedding utilities and implementations</li> <li>Knowledge Base - Knowledge base and lexicon management</li> <li>Metrics - Evaluation metrics for text classification</li> <li>Neural Networks - Neural network models and training utilities</li> <li>Plotting - Visualization utilities</li> <li>Utils - General utility functions</li> </ul>"},{"location":"api/#quick-links","title":"Quick Links","text":"<ul> <li>GitHub Repository</li> <li>PyPI Package</li> </ul>"},{"location":"api/embeddings/","title":"Embeddings","text":"<p>Text embedding utilities and implementations for converting text into dense vector representations.</p>"},{"location":"api/embeddings/#base-classes","title":"Base Classes","text":""},{"location":"api/embeddings/#tklearn.embeddings.base","title":"<code>base</code>","text":""},{"location":"api/embeddings/#tklearn.embeddings.base-classes","title":"Classes","text":""},{"location":"api/embeddings/#tklearn.embeddings.base.EmbeddingConfig","title":"<code>EmbeddingConfig</code>","text":"<p>               Bases: <code>BaseConfig</code></p> <p>Configuration for loading and managing embeddings.</p> <p>Attributes:</p> Name Type Description <code>loader</code> <code>str</code> <p>The identifier for the embedding loader module to use. This is a ClassVar and also the dispatch key for AutoModule.</p> <code>name</code> <code>str</code> <p>A unique name identifying the specific embedding set (e.g., 'glove-wiki-gigaword-100').</p> <code>verbose</code> <code>(bool or int, optional)</code> <p>Controls the verbosity of loading and processing messages. Defaults to 1.</p>"},{"location":"api/embeddings/#tklearn.embeddings.base.AutoEmbedding","title":"<code>AutoEmbedding</code>","text":"<p>               Bases: <code>AutoModule</code></p> <p>Factory class for creating Embedding instances based on configuration.</p> <p>Uses the 'loader' field in the EmbeddingConfig to dispatch to the appropriate Embedding subclass.</p>"},{"location":"api/embeddings/#tklearn.embeddings.base.AutoEmbedding-functions","title":"Functions","text":""},{"location":"api/embeddings/#tklearn.embeddings.base.AutoEmbedding.__new__","title":"<code>__new__(config)</code>","text":"<p>Instantiate an Embedding from a configuration.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>EmbeddingConfig or Mapping or str</code> <p>The configuration object, dictionary, or string identifier. If a string is provided, it's assumed to be the name, and 'gensim' loader is used by default. If a Mapping is provided, it's converted to an EmbeddingConfig.</p> required <p>Returns:</p> Type Description <code>Embedding</code> <p>An instance of the appropriate Embedding subclass.</p> Source code in <code>src/tklearn/embeddings/base.py</code> <pre><code>def __new__(cls, config: EmbeddingConfig | Mapping | str) -&gt; Embedding:\n    \"\"\"Instantiate an Embedding from a configuration.\n\n    Parameters\n    ----------\n    config : EmbeddingConfig or Mapping or str\n        The configuration object, dictionary, or string identifier.\n        If a string is provided, it's assumed to be the name, and\n        'gensim' loader is used by default. If a Mapping is provided,\n        it's converted to an EmbeddingConfig.\n\n    Returns\n    -------\n    Embedding\n        An instance of the appropriate Embedding subclass.\n    \"\"\"\n    if isinstance(config, str):\n        config = {\"loader\": \"gensim\", \"name\": config}\n    if not isinstance(config, EmbeddingConfig):\n        try:\n            config = EmbeddingConfig.from_dict(config)\n        except KeyError:\n            config = {\"loader\": \"gensim\", **config}\n            config = EmbeddingConfig.from_dict(config)\n    return super().__new__(cls, config)\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.base.Encodable","title":"<code>Encodable</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Protocol defining the interface for text encoding models.</p> <p>Classes implementing this protocol should provide a method to convert text strings into numerical vector representations (embeddings).</p>"},{"location":"api/embeddings/#tklearn.embeddings.base.Encodable-functions","title":"Functions","text":""},{"location":"api/embeddings/#tklearn.embeddings.base.Encodable.encode","title":"<code>encode(texts, **kwargs)</code>","text":"<p>Encode a given text string or token span into a vector.</p> <p>Parameters:</p> Name Type Description Default <code>texts</code> <code>str or list[str]</code> <p>The text to be encoded. Can be a single string or a list of strings (e.g., tokens or phrases).</p> required <code>**kwargs</code> <p>Additional keyword arguments for the encoding process. This may include options like batch size or other model-specific parameters.</p> <code>{}</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>The numerical vector representation (embedding) of the input texts.</p> Source code in <code>src/tklearn/embeddings/base.py</code> <pre><code>def encode(self, texts: str | list[str], **kwargs) -&gt; np.ndarray:\n    \"\"\"Encode a given text string or token span into a vector.\n\n    Parameters\n    ----------\n    texts : str or list[str]\n        The text to be encoded. Can be a single string or a list of\n        strings (e.g., tokens or phrases).\n    **kwargs\n        Additional keyword arguments for the encoding process.\n        This may include options like batch size or other model-specific\n        parameters.\n\n    Returns\n    -------\n    np.ndarray\n        The numerical vector representation (embedding) of the input texts.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.base.Encodable.get_dimension","title":"<code>get_dimension()</code>","text":"<p>Get the size of the embedding vector.</p> <p>Returns:</p> Type Description <code>int or None</code> <p>The dimensionality of the embedding vectors produced by this model.</p> Source code in <code>src/tklearn/embeddings/base.py</code> <pre><code>def get_dimension(self) -&gt; int | None:\n    \"\"\"Get the size of the embedding vector.\n\n    Returns\n    -------\n    int or None\n        The dimensionality of the embedding vectors produced by this model.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.base.EmbeddingBase","title":"<code>EmbeddingBase</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for embedding loaders.</p> <p>Defines the core interface that concrete embedding loader implementations must provide.</p>"},{"location":"api/embeddings/#tklearn.embeddings.base.EmbeddingBase-functions","title":"Functions","text":""},{"location":"api/embeddings/#tklearn.embeddings.base.EmbeddingBase.get_vectors","title":"<code>get_vectors()</code>","text":"<p>Load or compute the word-to-vector mapping.</p> <p>This method should handle the retrieval or generation of the embedding vectors from the source (e.g., file, model).</p> <p>Returns:</p> Type Description <code>dict[str, ArrayLike]</code> <p>A dictionary mapping words (or entities) to their corresponding numerical vector representations.</p> Source code in <code>src/tklearn/embeddings/base.py</code> <pre><code>def get_vectors(self) -&gt; dict[str, ArrayLike]:\n    \"\"\"Load or compute the word-to-vector mapping.\n\n    This method should handle the retrieval or generation of the\n    embedding vectors from the source (e.g., file, model).\n\n    Returns\n    -------\n    dict[str, ArrayLike]\n        A dictionary mapping words (or entities) to their corresponding\n        numerical vector representations.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.base.EmbeddingBase.get_encoder","title":"<code>get_encoder()</code>","text":"<p>Get the text encoder associated with these embeddings.</p> <p>This method should return an object conforming to the Encodable protocol, capable of encoding arbitrary text based on the loaded embedding model.</p> <p>Returns:</p> Type Description <code>Encodable</code> <p>An object that can encode text into vectors.</p> Source code in <code>src/tklearn/embeddings/base.py</code> <pre><code>def get_encoder(self) -&gt; Encodable:\n    \"\"\"Get the text encoder associated with these embeddings.\n\n    This method should return an object conforming to the Encodable\n    protocol, capable of encoding arbitrary text based on the loaded\n    embedding model.\n\n    Returns\n    -------\n    Encodable\n        An object that can encode text into vectors.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.base.Embedding","title":"<code>Embedding</code>","text":"<p>               Bases: <code>BaseModule</code>, <code>Mapping[str, ndarray]</code>, <code>EmbeddingBase</code></p> <p>Represents a loaded set of word embeddings.</p> <p>Provides access to word vectors via a mapping interface and potentially an underlying text encoding model. Handles caching of processed vectors.</p> <p>Attributes:</p> Name Type Description <code>config</code> <code>EmbeddingConfig</code> <p>The configuration used to load these embeddings.</p> <code>word_to_index</code> <code>dict[str, int] or None</code> <p>A mapping from words (vocabulary) to their integer index in the <code>vectors</code> array. Initialized during loading.</p> <code>vectors</code> <code>ndarray or None</code> <p>A 2D NumPy array where each row corresponds to a word vector, ordered according to <code>word_to_index</code>. Initialized during loading. May be a memory-mapped array for efficiency.</p> <code>model</code> <code>TextEncoder or None</code> <p>An optional underlying text encoding model (conforming to TextEncoder) that might be used for generating vectors for out-of-vocabulary words or for more complex encoding tasks. Initialized during loading.</p>"},{"location":"api/embeddings/#tklearn.embeddings.base.Embedding-attributes","title":"Attributes","text":""},{"location":"api/embeddings/#tklearn.embeddings.base.Embedding.shape","title":"<code>shape</code>  <code>property</code>","text":"<p>Return the shape of the underlying vector matrix (vocabulary size, dimension).</p>"},{"location":"api/embeddings/#tklearn.embeddings.base.Embedding-functions","title":"Functions","text":""},{"location":"api/embeddings/#tklearn.embeddings.base.Embedding.__post_init__","title":"<code>__post_init__()</code>","text":"<p>Initializes the embedding by loading or creating cached data.</p> Source code in <code>src/tklearn/embeddings/base.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    \"\"\"Initializes the embedding by loading or creating cached data.\"\"\"\n    cache_path = (\n        Path(config.assets_dir)\n        / self.config.loader\n        / \"data\"\n        / f\"vectors-{self.config.name}.data\"\n    )\n    # create embedding if not exists\n    cache_path.parent.mkdir(parents=True, exist_ok=True)\n    try:\n        model = self.get_encoder()\n        if model and not isinstance(model, Encodable):\n            msg = f\"{model.__class__.__name__} is not an instance of Encodable\"\n            warnings.warn(msg, UserWarning)\n            raise NotImplementedError\n        self.model = model\n    except NotImplementedError:\n        self.model = None\n    try:\n        self._load(cache_path)\n    except FileNotFoundError:\n        mapping = self.get_vectors()\n        self._from_dict(mapping)\n        self._dump(cache_path)\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.base.Embedding.encode","title":"<code>encode(key, **kwargs)</code>","text":"<p>Get the vector for a word, potentially using the model for OOV words.</p> <p>If an underlying <code>model</code> (TextEncoder) is available, it might be used to generate vectors for words not directly in the <code>vectors</code> table or to handle multi-word expressions by averaging sub-word vectors. Otherwise, it falls back to <code>__getitem__</code>. Results are cached.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str or list[str]</code> <p>The word or phrase to get the vector for.</p> required <code>**kwargs</code> <p>Additional keyword arguments for the encoder.</p> <code>{}</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>The vector representation of the word.</p> Source code in <code>src/tklearn/embeddings/base.py</code> <pre><code>def encode(self, key: str | list[str], **kwargs) -&gt; np.ndarray:\n    \"\"\"Get the vector for a word, potentially using the model for OOV words.\n\n    If an underlying `model` (TextEncoder) is available, it might be used\n    to generate vectors for words not directly in the `vectors` table or\n    to handle multi-word expressions by averaging sub-word vectors.\n    Otherwise, it falls back to `__getitem__`. Results are cached.\n\n    Parameters\n    ----------\n    key : str or list[str]\n        The word or phrase to get the vector for.\n    **kwargs\n        Additional keyword arguments for the encoder.\n\n    Returns\n    -------\n    np.ndarray\n        The vector representation of the word.\n    \"\"\"\n    try:\n        # this will be a 1D array\n        if not isinstance(key, str):\n            # this will be a 2D array\n            raise KeyError(key)\n        return self.vectors[self.word_to_index[key]]\n    except KeyError:\n        pass\n    if self.model:\n        vectors = self.model.encode(key, **kwargs)\n        if isinstance(key, str):\n            # this will be a 1D array\n            return vectors[0]\n        # this will be a 2D array\n        return vectors\n    raise KeyError(key)\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.base.Embedding.encode_query","title":"<code>encode_query(texts, **kwargs)</code>","text":"<p>Encode the texts as queries.</p> Source code in <code>src/tklearn/embeddings/base.py</code> <pre><code>def encode_query(self, texts: str | list[str], **kwargs) -&gt; np.ndarray:\n    \"\"\"Encode the texts as queries.\"\"\"\n    if not hasattr(self.model, \"encode_query\"):\n        raise NotImplementedError(\n            f\"{self.model.__class__.__name__} does not implement encode_query\"\n        )\n    return self.model.encode_query(texts, **kwargs)\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.base.Embedding.encode_document","title":"<code>encode_document(texts, **kwargs)</code>","text":"<p>Encode the texts as documents.</p> Source code in <code>src/tklearn/embeddings/base.py</code> <pre><code>def encode_document(self, texts: str | list[str], **kwargs) -&gt; np.ndarray:\n    \"\"\"Encode the texts as documents.\"\"\"\n    if not hasattr(self.model, \"encode_document\"):\n        raise NotImplementedError(\n            f\"{self.model.__class__.__name__} does not implement encode_document\"\n        )\n    return self.model.encode_document(texts, **kwargs)\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.base.Embedding.__getitem__","title":"<code>__getitem__(key)</code>","text":"<p>Retrieve the vector for a specific word.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>The text whose vector is requested.</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>The vector corresponding to the provided text.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If the word is not in the vocabulary (<code>word_to_index</code>) and the model is None.</p> Source code in <code>src/tklearn/embeddings/base.py</code> <pre><code>def __getitem__(self, key: str) -&gt; np.ndarray:\n    \"\"\"Retrieve the vector for a specific word.\n\n    Parameters\n    ----------\n    key : str\n        The text whose vector is requested.\n\n    Returns\n    -------\n    np.ndarray\n        The vector corresponding to the provided text.\n\n    Raises\n    ------\n    KeyError\n        If the word is not in the vocabulary (`word_to_index`) and the model is None.\n    \"\"\"\n    return self.encode(key)\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.base.Embedding.__iter__","title":"<code>__iter__()</code>","text":"<p>Iterate over the words in the vocabulary.</p> Source code in <code>src/tklearn/embeddings/base.py</code> <pre><code>def __iter__(self) -&gt; Iterable[str]:\n    \"\"\"Iterate over the words in the vocabulary.\"\"\"\n    return iter(self.word_to_index)\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.base.Embedding.__len__","title":"<code>__len__()</code>","text":"<p>Return the number of words in the vocabulary.</p> Source code in <code>src/tklearn/embeddings/base.py</code> <pre><code>def __len__(self) -&gt; int:\n    \"\"\"Return the number of words in the vocabulary.\"\"\"\n    return len(self.word_to_index)\n</code></pre>"},{"location":"api/embeddings/#fasttext-embeddings","title":"FastText Embeddings","text":""},{"location":"api/embeddings/#tklearn.embeddings.fasttext","title":"<code>fasttext</code>","text":""},{"location":"api/embeddings/#tklearn.embeddings.fasttext-classes","title":"Classes","text":""},{"location":"api/embeddings/#tklearn.embeddings.fasttext.FastTextWrapper","title":"<code>FastTextWrapper(model)</code>","text":"Source code in <code>src/tklearn/embeddings/fasttext.py</code> <pre><code>def __init__(self, model: fasttext.FastText._FastText):\n    self.model = model\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.fasttext.FastTextWrapper-functions","title":"Functions","text":""},{"location":"api/embeddings/#tklearn.embeddings.fasttext.FastTextWrapper.encode","title":"<code>encode(texts)</code>","text":"<p>Encode the texts.</p> Source code in <code>src/tklearn/embeddings/fasttext.py</code> <pre><code>def encode(self, texts: str | list[str]) -&gt; np.ndarray:\n    \"\"\"Encode the texts.\"\"\"\n    if isinstance(texts, str):\n        texts = [texts]\n    vectors = []\n    for text in texts:\n        vectors.append(self.model.get_word_vector(text))\n    return np.array(vectors)\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.fasttext.FastTextWrapper.get_dimension","title":"<code>get_dimension()</code>","text":"<p>Get the embedding size.</p> Source code in <code>src/tklearn/embeddings/fasttext.py</code> <pre><code>def get_dimension(self) -&gt; int | None:\n    \"\"\"Get the embedding size.\"\"\"\n    return self.model.get_dimension()\n</code></pre>"},{"location":"api/embeddings/#gensim-embeddings","title":"Gensim Embeddings","text":""},{"location":"api/embeddings/#tklearn.embeddings.gensim","title":"<code>gensim</code>","text":""},{"location":"api/embeddings/#tklearn.embeddings.gensim-classes","title":"Classes","text":""},{"location":"api/embeddings/#tklearn.embeddings.gensim.GensimEmbedding","title":"<code>GensimEmbedding</code>","text":"<p>               Bases: <code>Embedding</code></p>"},{"location":"api/embeddings/#tklearn.embeddings.gensim.GensimEmbedding-functions","title":"Functions","text":""},{"location":"api/embeddings/#tklearn.embeddings.gensim.GensimEmbedding.get_encoder","title":"<code>get_encoder()</code>","text":"<p>Returns the encoder for the embedding.</p> Source code in <code>src/tklearn/embeddings/gensim.py</code> <pre><code>def get_encoder(self) -&gt; GensimModelWrapper:\n    \"\"\"Returns the encoder for the embedding.\"\"\"\n    model: KeyedVectors\n    with change_dir(self.files_dir):\n        model = api.load(self.config.name)\n    return GensimModelWrapper(model)\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.gensim.GensimModelWrapper","title":"<code>GensimModelWrapper(model)</code>","text":"Source code in <code>src/tklearn/embeddings/gensim.py</code> <pre><code>def __init__(self, model: KeyedVectors):\n    self.model = model\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.gensim.GensimModelWrapper-functions","title":"Functions","text":""},{"location":"api/embeddings/#tklearn.embeddings.gensim.GensimModelWrapper.encode","title":"<code>encode(texts)</code>","text":"<p>Encode a given text string or token span into a vector.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str or list[str]</code> <p>The text to be encoded. Can be a single string or a list of strings (e.g., tokens or phrases).</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>The numerical vector representation (embedding) of the input text.</p> Source code in <code>src/tklearn/embeddings/gensim.py</code> <pre><code>def encode(self, texts: str | list[str]) -&gt; np.ndarray:\n    \"\"\"Encode a given text string or token span into a vector.\n\n    Parameters\n    ----------\n    text : str or list[str]\n        The text to be encoded. Can be a single string or a list of\n        strings (e.g., tokens or phrases).\n\n    Returns\n    -------\n    np.ndarray\n        The numerical vector representation (embedding) of the input text.\n    \"\"\"\n    if isinstance(texts, str):\n        texts = [texts]\n    vectors = []\n    for text in texts:\n        vectors.append(self.model[text])\n    return np.array(vectors)\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.gensim.GensimModelWrapper.get_dimension","title":"<code>get_dimension()</code>","text":"<p>Get the size of the embedding vector.</p> <p>Returns:</p> Type Description <code>int</code> <p>The dimensionality of the embedding vectors produced by this model.</p> Source code in <code>src/tklearn/embeddings/gensim.py</code> <pre><code>def get_dimension(self) -&gt; int | None:\n    \"\"\"Get the size of the embedding vector.\n\n    Returns\n    -------\n    int\n        The dimensionality of the embedding vectors produced by this model.\n    \"\"\"\n    return self.model.vector_size\n</code></pre>"},{"location":"api/embeddings/#transformer-embeddings","title":"Transformer Embeddings","text":""},{"location":"api/embeddings/#tklearn.embeddings.transformers","title":"<code>transformers</code>","text":""},{"location":"api/embeddings/#tklearn.embeddings.transformers-classes","title":"Classes","text":""},{"location":"api/embeddings/#tklearn.embeddings.transformers.SentenceTransformerEmbedding","title":"<code>SentenceTransformerEmbedding</code>","text":"<p>               Bases: <code>Embedding</code></p>"},{"location":"api/embeddings/#tklearn.embeddings.transformers.SentenceTransformerEmbedding-functions","title":"Functions","text":""},{"location":"api/embeddings/#tklearn.embeddings.transformers.SentenceTransformerEmbedding.get_vectors","title":"<code>get_vectors()</code>","text":"<p>Load resource.</p> Source code in <code>src/tklearn/embeddings/transformers.py</code> <pre><code>def get_vectors(self) -&gt; dict[str, ArrayLike]:\n    \"\"\"Load resource.\"\"\"\n    return {}\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.transformers.SentenceTransformerEmbedding.get_encoder","title":"<code>get_encoder()</code>","text":"<p>Return the model.</p> Source code in <code>src/tklearn/embeddings/transformers.py</code> <pre><code>def get_encoder(self) -&gt; TransformerWrapper:\n    \"\"\"Return the model.\"\"\"\n    device = get_device(self.config.device)\n    encoder = SentenceTransformer(self.config.name, device=device)\n    return TransformerWrapper(encoder)\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.transformers.TransformerWrapper","title":"<code>TransformerWrapper(model)</code>","text":"Source code in <code>src/tklearn/embeddings/transformers.py</code> <pre><code>def __init__(self, model: SentenceTransformer):\n    self.model = model\n    self.batch_size = 32  # type: int\n    self.show_progress_bar = False  # type: bool\n    self.encoder = None  # type: str | None\n    self.convert_to_numpy = True  # type: bool\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.transformers.TransformerWrapper-functions","title":"Functions","text":""},{"location":"api/embeddings/#tklearn.embeddings.transformers.TransformerWrapper.encode","title":"<code>encode(texts, **kwargs)</code>","text":"<p>Encode the texts.</p> Source code in <code>src/tklearn/embeddings/transformers.py</code> <pre><code>def encode(self, texts: str | list[str], **kwargs) -&gt; np.ndarray:\n    \"\"\"Encode the texts.\"\"\"\n    return self._encode(texts, encoder=None, kwargs=kwargs)\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.transformers.TransformerWrapper.encode_query","title":"<code>encode_query(texts, **kwargs)</code>","text":"<p>Encode the texts as queries.</p> Source code in <code>src/tklearn/embeddings/transformers.py</code> <pre><code>def encode_query(self, texts: str | list[str], **kwargs) -&gt; np.ndarray:\n    \"\"\"Encode the texts as queries.\"\"\"\n    return self._encode(texts, encoder=\"query\", kwargs=kwargs)\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.transformers.TransformerWrapper.encode_document","title":"<code>encode_document(texts, **kwargs)</code>","text":"<p>Encode the texts as documents.</p> Source code in <code>src/tklearn/embeddings/transformers.py</code> <pre><code>def encode_document(self, texts: str | list[str], **kwargs) -&gt; np.ndarray:\n    \"\"\"Encode the texts as documents.\"\"\"\n    return self._encode(texts, encoder=\"document\", kwargs=kwargs)\n</code></pre>"},{"location":"api/embeddings/#tklearn.embeddings.transformers.TransformerWrapper.get_dimension","title":"<code>get_dimension()</code>","text":"<p>Get the embedding size.</p> Source code in <code>src/tklearn/embeddings/transformers.py</code> <pre><code>def get_dimension(self) -&gt; int:\n    \"\"\"Get the embedding size.\"\"\"\n    return self.model.get_sentence_embedding_dimension()\n</code></pre>"},{"location":"api/kb/","title":"Knowledge Base","text":"<p>Knowledge base management, lexicons, and triple stores for integrating external knowledge into NLP models.</p>"},{"location":"api/kb/#base-classes","title":"Base Classes","text":""},{"location":"api/kb/#tklearn.kb.base","title":"<code>base</code>","text":""},{"location":"api/kb/#tklearn.kb.base-classes","title":"Classes","text":""},{"location":"api/kb/#tklearn.kb.base.KnowledgeBase","title":"<code>KnowledgeBase(config, **kwargs)</code>","text":"Source code in <code>src/tklearn/kb/base.py</code> <pre><code>def __init__(self, config: Any, **kwargs: Any) -&gt; None:\n    self.store = AutoArtifactStore(config, **kwargs)\n</code></pre>"},{"location":"api/kb/#tklearn.kb.base.KnowledgeBase-functions","title":"Functions","text":""},{"location":"api/kb/#tklearn.kb.base.KnowledgeBase.extract_candidates","title":"<code>extract_candidates(word)</code>","text":"<p>Get all words and senses for a given form.</p> Source code in <code>src/tklearn/kb/base.py</code> <pre><code>def extract_candidates(\n    self, word: str | tuple[str, Any]\n) -&gt; Iterable[Candidate]:\n    \"\"\"Get all words and senses for a given form.\"\"\"\n    # get all the senses of the word\n    sense_id = None\n    if isinstance(word, tuple):\n        word, sense_id = word\n    if sense_id is not None:\n        gloss = self.idx2gloss[sense_id]\n        embedding = self.embeddings.get(sense_id, None)\n        yield Candidate(word, gloss, embedding, sense_id).bind(self)\n        return\n    for sense_id in self.senses.get(word, None) or []:\n        gloss = self.idx2gloss[sense_id]\n        embedding = self.embeddings.get(sense_id, None)\n        yield Candidate(word, gloss, embedding, sense_id).bind(self)\n</code></pre>"},{"location":"api/kb/#tklearn.kb.base.KnowledgeBase.extract_mentions","title":"<code>extract_mentions(text, min_word_len=3, stopwords='english')</code>","text":"<p>Analyze text and return a list of words and their senses.</p> Source code in <code>src/tklearn/kb/base.py</code> <pre><code>def extract_mentions(\n    self,\n    text: str,\n    min_word_len: int = 3,\n    stopwords: Iterable[str] | str = \"english\",\n) -&gt; Iterable[Mention]:\n    \"\"\"Analyze text and return a list of words and their senses.\"\"\"\n    stopwords = get_stopwords(stopwords)\n    stopwords = set(stopwords)\n    for words, start, end in self.lexicon.extract(text):\n        candidates = []\n        form = text[start:end]\n        if len(form) &lt; min_word_len:\n            continue\n        if form.lower() in stopwords:\n            continue\n        for word in words:\n            for cc in self.extract_candidates(word):\n                # TODO: remove comment below if not needed\n                # if cc.word.lower() != form.lower():\n                #     continue\n                candidates.append(cc)\n        yield Mention(form, Span(start, end), candidates)\n</code></pre>"},{"location":"api/kb/#tklearn.kb.base.KnowledgeBase.augment","title":"<code>augment(text, mentions=None, relation_filter=None)</code>","text":"<p>Augment text by replacing words with synonyms, hyponyms, and hypernyms.</p> Source code in <code>src/tklearn/kb/base.py</code> <pre><code>def augment(\n    self, text: str, mentions=None, relation_filter=None\n) -&gt; Iterable[dict[str, Any]]:\n    \"\"\"Augment text by replacing words with synonyms, hyponyms, and hypernyms.\"\"\"\n    relation_filter = relation_filter or (lambda x: True)\n    # not augmented\n    yield {\n        \"text\": text,\n        \"original\": text,\n        \"span.start\": 0,\n        \"span.end\": 0,\n        \"relations\": [],\n        \"support\": 0,\n    }\n    if mentions is None:\n        mentions = self.extract_mentions(text)\n    for mention in mentions:\n        start, end = mention.span\n        prefix, suffix = text[:start], text[end:]\n        aug_words = defaultdict(set)\n        for candidate in mention.candidates:\n            subject = (candidate.word, candidate.sense_id)\n            for predicate in [\"synonym\", \"hyponym\", \"instance\"]:\n                objects = self.triples.get(subject, {}).get(predicate, [])\n                for object_ in objects:\n                    rel = Triple(subject, predicate, object_)\n                    if not relation_filter(rel):\n                        continue\n                    aug_words[rel.object[0]].add(rel.to_tuple())\n        for word, relations in aug_words.items():\n            augmented_text = None\n            augmented_text = prefix + word + suffix\n            yield {\n                \"text\": augmented_text,\n                \"original\": text,\n                \"span.start\": start,\n                \"span.end\": end,\n                \"relations\": list(relations),\n                \"support\": len(relations),\n            }\n</code></pre>"},{"location":"api/kb/#tklearn.kb.base-functions","title":"Functions","text":""},{"location":"api/kb/#tklearn.kb.base.get_candidate","title":"<code>get_candidate(kb, word, sense_id)</code>","text":"<p>Retrieve a Candidate object for the given word and sense_id.</p> Source code in <code>src/tklearn/kb/base.py</code> <pre><code>def get_candidate(kb: KnowledgeBase, word: str, sense_id: int) -&gt; Candidate:\n    \"\"\"Retrieve a Candidate object for the given word and sense_id.\"\"\"\n    gloss = kb.idx2gloss[sense_id]\n    embedding = kb.embeddings[sense_id]\n    return Candidate(word, gloss, embedding, sense_id)\n</code></pre>"},{"location":"api/kb/#lexicon","title":"Lexicon","text":""},{"location":"api/kb/#tklearn.kb.lexicon","title":"<code>lexicon</code>","text":"<p>Lexicon class for keyword extraction and replacement.</p> <p>This module provides a Lexicon class that uses a trie data structure to efficiently find and replace keywords in text. The Lexicon class supports case sensitivity, tokenization, and the ability to build failure links for efficient matching. It also provides methods for adding, deleting, and extracting keywords, as well as displaying the trie structure.</p>"},{"location":"api/kb/#tklearn.kb.lexicon-classes","title":"Classes","text":""},{"location":"api/kb/#tklearn.kb.lexicon.TrieNode","title":"<code>TrieNode(value=UNDEFINED, children=dict(), fail=None, depth=None)</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Generic[T]</code></p> <p>A node in a trie data structure used for keyword matching.</p> <p>This class implements a node for a trie (prefix tree) that is used in the Aho-Corasick string matching algorithm. Each node can store a value and maintains references to child nodes, a failure link for the Aho-Corasick algorithm, and its depth in the trie.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Optional[T]</code> <p>The value associated with this node. UNDEFINED indicates no value is stored.</p> <code>UNDEFINED</code> <code>children</code> <code>dict[str, TrieNode]</code> <p>Dictionary mapping characters to child nodes.</p> <code>empty dict</code> <code>fail</code> <code>Optional[TrieNode]</code> <p>Reference to the failure node for the Aho-Corasick algorithm.</p> <code>None</code> <code>depth</code> <code>Optional[int]</code> <p>The depth of this node in the trie.</p> <code>None</code> <p>Attributes:</p> Name Type Description <code>value</code> <code>Optional[T]</code> <p>The value stored in this node, if any.</p> <code>children</code> <code>dict[str, TrieNode]</code> <p>Dictionary of child nodes, where keys are characters and values are TrieNodes.</p> <code>fail</code> <code>Optional[TrieNode]</code> <p>Failure link used in the Aho-Corasick algorithm for efficient pattern matching.</p> <code>depth</code> <code>Optional[int]</code> <p>The depth of this node in the trie structure.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; root = TrieNode[str]()  # Create root node\n&gt;&gt;&gt; child = TrieNode[str](value=\"example\")  # Create child with value\n&gt;&gt;&gt; root.children[\"a\"] = child  # Add child node\n&gt;&gt;&gt; child.fail = root  # Set failure link\n&gt;&gt;&gt; child.depth = 1  # Set depth\n</code></pre>"},{"location":"api/kb/#tklearn.kb.lexicon.Lexicon","title":"<code>Lexicon(case_sensitive=False)</code>","text":"<p>               Bases: <code>Generic[T]</code>, <code>MutableMapping[str, T]</code></p> <p>A generic keyword processor using a trie data structure.</p> <p>This class implements a keyword processor that uses a trie (prefix tree) data structure for efficient keyword matching and extraction from text. It supports case-sensitive and case-insensitive matching, and implements the MutableMapping interface for dictionary-like operations.</p> <p>Parameters:</p> Name Type Description Default <code>case_sensitive</code> <code>bool</code> <p>Whether to perform case-sensitive matching of keywords.</p> <code>False</code> <p>Attributes:</p> Name Type Description <code>_root</code> <code>TrieNode[T]</code> <p>The root node of the trie data structure.</p> <code>_len</code> <code>int</code> <p>Number of keywords stored in the lexicon.</p> <code>_updated</code> <code>bool</code> <p>Flag indicating if the trie needs rebuilding of failure links.</p> <code>_case_sensitive</code> <code>bool</code> <p>Flag indicating if keyword matching is case-sensitive.</p> Notes <p>The Lexicon class uses the Aho-Corasick algorithm for efficient pattern matching, which is built upon a trie data structure with failure links. This allows for O(n + m + k) time complexity for pattern matching, where n is the length of the text, m is the total length of the patterns, and k is the number of matches.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; # Create a case-insensitive lexicon\n&gt;&gt;&gt; lexicon = Lexicon[str]()\n&gt;&gt;&gt; # Add some keywords with their associated values\n&gt;&gt;&gt; lexicon[\"New York\"] = \"NY\"\n&gt;&gt;&gt; lexicon[\"Los Angeles\"] = \"LA\"\n&gt;&gt;&gt; # Extract keywords from text\n&gt;&gt;&gt; text = \"I love New York and Los Angeles!\"\n&gt;&gt;&gt; for value, start, end in lexicon.extract(text):\n...     print(f\"Found {value} at positions {start}-{end}\")\nFound NY at positions 7-15\nFound LA at positions 20-30\n</code></pre> <p>Initialize a new Lexicon instance.</p> <p>Parameters:</p> Name Type Description Default <code>case_sensitive</code> <code>bool</code> <p>Whether to perform case-sensitive matching of keywords.</p> <code>False</code> Source code in <code>src/tklearn/kb/lexicon.py</code> <pre><code>def __init__(self, case_sensitive: bool = False):\n    \"\"\"Initialize a new Lexicon instance.\n\n    Parameters\n    ----------\n    case_sensitive : bool, default=False\n        Whether to perform case-sensitive matching of keywords.\n    \"\"\"\n    self._case_sensitive = case_sensitive\n    self._root = TrieNode()\n    self._len = 0\n    self._updated = False\n</code></pre>"},{"location":"api/kb/#tklearn.kb.lexicon.Lexicon-functions","title":"Functions","text":""},{"location":"api/kb/#tklearn.kb.lexicon.Lexicon.tokeinze","title":"<code>tokeinze(texts, return_offsets=False)</code>","text":"<p>Tokenize input texts into words with optional position offsets.</p> <p>Parameters:</p> Name Type Description Default <code>texts</code> <code>list[str]</code> <p>List of input texts to tokenize.</p> required <code>return_offsets</code> <code>bool</code> <p>Whether to return token position offsets in the original text.</p> <code>False</code> <p>Yields:</p> Type Description <code>Union[list[str], tuple[list[str], list[tuple[int, int]]]]</code> <p>If return_offsets is False, yields list of tokens for each text. If return_offsets is True, yields tuple of (tokens, offsets) where offsets is a list of (start, end) positions for each token.</p> Notes <p>Uses the unibreak library for Unicode-aware word segmentation. Handles case conversion based on the lexicon's case_sensitive setting.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; lexicon = Lexicon()\n&gt;&gt;&gt; # Without offsets\n&gt;&gt;&gt; list(lexicon.tokeinze([\"Hello World!\"]))\n[['hello', 'world']]\n&gt;&gt;&gt; # With offsets\n&gt;&gt;&gt; list(lexicon.tokeinze([\"Hello World!\"], return_offsets=True))\n[(['hello', 'world'], [(0, 5), (6, 11)])]\n</code></pre> Source code in <code>src/tklearn/kb/lexicon.py</code> <pre><code>def tokeinze(self, texts: list[str], return_offsets: bool = False):\n    \"\"\"Tokenize input texts into words with optional position offsets.\n\n    Parameters\n    ----------\n    texts : list[str]\n        List of input texts to tokenize.\n    return_offsets : bool, default=False\n        Whether to return token position offsets in the original text.\n\n    Yields\n    ------\n    Union[list[str], tuple[list[str], list[tuple[int, int]]]]\n        If return_offsets is False, yields list of tokens for each text.\n        If return_offsets is True, yields tuple of (tokens, offsets) where\n        offsets is a list of (start, end) positions for each token.\n\n    Notes\n    -----\n    Uses the unibreak library for Unicode-aware word segmentation.\n    Handles case conversion based on the lexicon's case_sensitive setting.\n\n    Examples\n    --------\n    &gt;&gt;&gt; lexicon = Lexicon()\n    &gt;&gt;&gt; # Without offsets\n    &gt;&gt;&gt; list(lexicon.tokeinze([\"Hello World!\"]))\n    [['hello', 'world']]\n    &gt;&gt;&gt; # With offsets\n    &gt;&gt;&gt; list(lexicon.tokeinze([\"Hello World!\"], return_offsets=True))\n    [(['hello', 'world'], [(0, 5), (6, 11)])]\n    \"\"\"\n    if isinstance(texts, str):\n        raise TypeError(\"expected a list of strings, got a string\")\n    for text in texts:\n        tokens = []\n        offsets = []\n        start, end = 0, 0\n        # token may be a space here!\n        for token in unibreak.split_words(text):\n            start = end\n            end += len(token)\n            token = token.strip()\n            # remove spaces\n            if not token:\n                continue\n            if not self._case_sensitive:\n                token = token.lower()\n            tokens.append(token)\n            offsets.append((start, end))\n            start = end\n        if end != len(text):\n            # TODO: use regex to split the words\n            raise\n        if return_offsets:\n            yield tokens, offsets\n        else:\n            yield tokens\n</code></pre>"},{"location":"api/kb/#tklearn.kb.lexicon.Lexicon.keys","title":"<code>keys(prefix='')</code>","text":"<p>Return an iterator over all keywords with a given prefix.</p> <p>Parameters:</p> Name Type Description Default <code>prefix</code> <code>str</code> <p>The prefix to filter keywords. If empty, returns all keywords.</p> <code>\"\"</code> <p>Returns:</p> Type Description <code>Iterator[str]</code> <p>Iterator yielding all keywords that start with the given prefix.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; lexicon = Lexicon[str]()\n&gt;&gt;&gt; lexicon[\"apple\"] = \"fruit\"\n&gt;&gt;&gt; lexicon[\"application\"] = \"software\"\n&gt;&gt;&gt; list(lexicon.keys(\"app\"))\n['apple', 'application']\n</code></pre> Source code in <code>src/tklearn/kb/lexicon.py</code> <pre><code>def keys(self, prefix: str = \"\") -&gt; Iterator[str]:\n    \"\"\"Return an iterator over all keywords with a given prefix.\n\n    Parameters\n    ----------\n    prefix : str, default=\"\"\n        The prefix to filter keywords. If empty, returns all keywords.\n\n    Returns\n    -------\n    Iterator[str]\n        Iterator yielding all keywords that start with the given prefix.\n\n    Examples\n    --------\n    &gt;&gt;&gt; lexicon = Lexicon[str]()\n    &gt;&gt;&gt; lexicon[\"apple\"] = \"fruit\"\n    &gt;&gt;&gt; lexicon[\"application\"] = \"software\"\n    &gt;&gt;&gt; list(lexicon.keys(\"app\"))\n    ['apple', 'application']\n    \"\"\"\n    trie, tokens = self._get_node(prefix, return_tokens=True)\n    if trie is None:\n        return\n    prefix = \"\".join(tokens)\n    stack = [(prefix, trie)]\n    while stack:\n        prefix, trie = stack.pop()\n        if trie.value is not UNDEFINED:\n            yield prefix\n        for key, child in trie.children.items():\n            if prefix:\n                stack.append((f\"{prefix} {key}\", child))\n            else:\n                stack.append((f\"{key}\", child))\n</code></pre>"},{"location":"api/kb/#tklearn.kb.lexicon.Lexicon.__contains__","title":"<code>__contains__(key)</code>","text":"<p>Check if a keyword exists in the lexicon.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>The keyword to check.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the keyword exists in the lexicon, False otherwise.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; lexicon = Lexicon[str]()\n&gt;&gt;&gt; lexicon[\"python\"] = \"language\"\n&gt;&gt;&gt; \"python\" in lexicon\nTrue\n&gt;&gt;&gt; \"java\" in lexicon\nFalse\n</code></pre> Source code in <code>src/tklearn/kb/lexicon.py</code> <pre><code>def __contains__(self, key: str) -&gt; bool:\n    \"\"\"Check if a keyword exists in the lexicon.\n\n    Parameters\n    ----------\n    key : str\n        The keyword to check.\n\n    Returns\n    -------\n    bool\n        True if the keyword exists in the lexicon, False otherwise.\n\n    Examples\n    --------\n    &gt;&gt;&gt; lexicon = Lexicon[str]()\n    &gt;&gt;&gt; lexicon[\"python\"] = \"language\"\n    &gt;&gt;&gt; \"python\" in lexicon\n    True\n    &gt;&gt;&gt; \"java\" in lexicon\n    False\n    \"\"\"\n    trie = self._get_node(key)\n    if trie is None:\n        return False\n    return trie.value is not UNDEFINED\n</code></pre>"},{"location":"api/kb/#tklearn.kb.lexicon.Lexicon.__setitem__","title":"<code>__setitem__(key, value)</code>","text":"<p>Add or update a keyword with an associated value.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>The keyword to add or update.</p> required <code>value</code> <code>T</code> <p>The value to associate with the keyword.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the key is not a string or is empty.</p> Notes <p>Updates the trie structure and marks it for rebuilding failure links. If the keyword already exists, its value is updated.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; lexicon = Lexicon[str]()\n&gt;&gt;&gt; lexicon[\"python\"] = \"language\"  # Add new keyword\n&gt;&gt;&gt; lexicon[\"python\"] = \"snake\"     # Update existing keyword\n</code></pre> Source code in <code>src/tklearn/kb/lexicon.py</code> <pre><code>def __setitem__(self, key: str, value: T):\n    \"\"\"Add or update a keyword with an associated value.\n\n    Parameters\n    ----------\n    key : str\n        The keyword to add or update.\n    value : T\n        The value to associate with the keyword.\n\n    Raises\n    ------\n    ValueError\n        If the key is not a string or is empty.\n\n    Notes\n    -----\n    Updates the trie structure and marks it for rebuilding failure links.\n    If the keyword already exists, its value is updated.\n\n    Examples\n    --------\n    &gt;&gt;&gt; lexicon = Lexicon[str]()\n    &gt;&gt;&gt; lexicon[\"python\"] = \"language\"  # Add new keyword\n    &gt;&gt;&gt; lexicon[\"python\"] = \"snake\"     # Update existing keyword\n    \"\"\"\n    trie = self._root\n\n    if not isinstance(key, str):\n        raise ValueError(\"only strings are allowed as keys\")\n\n    # Split word into tokens (similar to split_word_bounds in Rust)\n    tokens = next(self.tokeinze([key]))\n\n    for token in tokens:\n        if token not in trie.children:\n            trie.children[token] = TrieNode()\n        trie = trie.children[token]\n\n    if trie == self._root:\n        msg = f\"the keyword '{key}' is empty\"\n        raise ValueError(msg)\n\n    # Increment len only if the keyword isn't already there\n    if trie.value is UNDEFINED:\n        self._len += 1\n    trie.value = value\n\n    # mark that the trie is updated\n    self._updated = True\n</code></pre>"},{"location":"api/kb/#tklearn.kb.lexicon.Lexicon.__delitem__","title":"<code>__delitem__(key)</code>","text":"<p>Remove a keyword from the lexicon.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>The keyword to remove.</p> required <p>Raises:</p> Type Description <code>KeyError</code> <p>If the keyword doesn't exist in the lexicon.</p> Notes <p>Removes the keyword and cleans up any unused nodes in the trie. Marks the trie for rebuilding failure links.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; lexicon = Lexicon[str]()\n&gt;&gt;&gt; lexicon[\"python\"] = \"language\"\n&gt;&gt;&gt; del lexicon[\"python\"]  # Remove keyword\n&gt;&gt;&gt; \"python\" in lexicon\nFalse\n</code></pre> Source code in <code>src/tklearn/kb/lexicon.py</code> <pre><code>def __delitem__(self, key: str):\n    \"\"\"Remove a keyword from the lexicon.\n\n    Parameters\n    ----------\n    key : str\n        The keyword to remove.\n\n    Raises\n    ------\n    KeyError\n        If the keyword doesn't exist in the lexicon.\n\n    Notes\n    -----\n    Removes the keyword and cleans up any unused nodes in the trie.\n    Marks the trie for rebuilding failure links.\n\n    Examples\n    --------\n    &gt;&gt;&gt; lexicon = Lexicon[str]()\n    &gt;&gt;&gt; lexicon[\"python\"] = \"language\"\n    &gt;&gt;&gt; del lexicon[\"python\"]  # Remove keyword\n    &gt;&gt;&gt; \"python\" in lexicon\n    False\n    \"\"\"\n    parents: list[tuple[TrieNode[T], str]] = []\n    trie = self._root\n\n    # Split word into tokens (similar to split_word_bounds in Rust)\n    tokens = next(self.tokeinze([key]))\n\n    for token in tokens:\n        if token not in trie.children:\n            raise KeyError(key)\n        parents.append((trie, token))\n        trie = trie.children[token]\n\n    # Decrement len only if the keyword is there\n    if trie.value is UNDEFINED:\n        raise KeyError(key)\n\n    self._len -= 1\n    trie.value = UNDEFINED\n\n    # Delete the nodes that are no longer needed\n    for parent, token in reversed(parents):\n        child = parent.children[token]\n        if child.value is not UNDEFINED or child.children:\n            break\n        # remove the useless child node\n        del parent.children[token]\n\n    # mark that the trie is updated\n    self._updated = True\n</code></pre>"},{"location":"api/kb/#tklearn.kb.lexicon.Lexicon.build","title":"<code>build(force=False)</code>","text":"<p>Build failure links in the trie using breadth-first search.</p> <p>Parameters:</p> Name Type Description Default <code>force</code> <code>bool</code> <p>If True, rebuilds failure links even if the trie hasn't been modified.</p> <code>False</code> Notes <p>This method implements the Aho-Corasick algorithm's preprocessing step, building failure links that enable efficient pattern matching. The failure links are automatically rebuilt when needed during extraction, so manual calls to this method are usually unnecessary.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; lexicon = Lexicon[str]()\n&gt;&gt;&gt; lexicon[\"cat\"] = \"animal\"\n&gt;&gt;&gt; lexicon[\"catch\"] = \"verb\"\n&gt;&gt;&gt; lexicon.build()  # Build failure links\n</code></pre> Source code in <code>src/tklearn/kb/lexicon.py</code> <pre><code>def build(self, force: bool = False):\n    \"\"\"Build failure links in the trie using breadth-first search.\n\n    Parameters\n    ----------\n    force : bool, default=False\n        If True, rebuilds failure links even if the trie hasn't been modified.\n\n    Notes\n    -----\n    This method implements the Aho-Corasick algorithm's preprocessing step,\n    building failure links that enable efficient pattern matching.\n    The failure links are automatically rebuilt when needed during extraction,\n    so manual calls to this method are usually unnecessary.\n\n    Examples\n    --------\n    &gt;&gt;&gt; lexicon = Lexicon[str]()\n    &gt;&gt;&gt; lexicon[\"cat\"] = \"animal\"\n    &gt;&gt;&gt; lexicon[\"catch\"] = \"verb\"\n    &gt;&gt;&gt; lexicon.build()  # Build failure links\n    \"\"\"\n    if not (self._updated or force):\n        return\n\n    queue = deque()\n    root = self._root\n    root.depth = 0\n\n    # Handle depth 1 nodes\n    for key, child in root.children.items():\n        queue.append(child)\n        child.fail = root\n        child.depth = 1\n\n    # BFS for remaining nodes\n    while queue:\n        current: TrieNode = queue.popleft()\n\n        for key, child in current.children.items():\n            queue.append(child)\n\n            child.depth = current.depth + 1\n\n            failure = current.fail\n            # get the root of the faliures\n            while failure is not None and key not in failure.children:\n                failure = failure.fail\n\n            child.fail = failure.children[key] if failure else root\n\n    self._updated = False\n</code></pre>"},{"location":"api/kb/#tklearn.kb.lexicon.Lexicon.extract","title":"<code>extract(text)</code>","text":"<pre><code>extract(text: str) -&gt; Iterator[tuple[T, int, int]]\n</code></pre><pre><code>extract(\n    text: list[str],\n) -&gt; Iterator[Iterator[tuple[T, int, int]]]\n</code></pre> <p>Extract keywords from text using the Aho-Corasick algorithm.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>Union[str, list[str]]</code> <p>Input text or list of texts to search for keywords.</p> required <p>Returns:</p> Type Description <code>Union[Iterator[tuple[T, int, int]], Iterator[Iterator[tuple[T, int, int]]]]</code> <p>For single text: Iterator yielding (value, start, end) tuples. For list of texts: Iterator of iterators, each yielding (value, start, end) tuples. - value: The value associated with the matched keyword - start: Starting position in the text - end: Ending position in the text</p> Notes <p>Automatically rebuilds failure links if the trie has been modified. Returns the longest possible matches when multiple keywords overlap.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; lexicon = Lexicon[str]()\n&gt;&gt;&gt; lexicon[\"cat\"] = \"animal\"\n&gt;&gt;&gt; lexicon[\"catches\"] = \"verb\"\n&gt;&gt;&gt; text = \"The cat catches mice\"\n&gt;&gt;&gt; for value, start, end in lexicon.extract(text):\n...     print(f\"Found {value} at {start}-{end}: {text[start:end]}\")\nFound animal at 4-7: cat\nFound verb at 8-15: catches\n</code></pre> Source code in <code>src/tklearn/kb/lexicon.py</code> <pre><code>def extract(self, text: Any) -&gt; Any:\n    \"\"\"Extract keywords from text using the Aho-Corasick algorithm.\n\n    Parameters\n    ----------\n    text : Union[str, list[str]]\n        Input text or list of texts to search for keywords.\n\n    Returns\n    -------\n    Union[Iterator[tuple[T, int, int]], Iterator[Iterator[tuple[T, int, int]]]]\n        For single text: Iterator yielding (value, start, end) tuples.\n        For list of texts: Iterator of iterators, each yielding (value, start, end) tuples.\n        - value: The value associated with the matched keyword\n        - start: Starting position in the text\n        - end: Ending position in the text\n\n    Notes\n    -----\n    Automatically rebuilds failure links if the trie has been modified.\n    Returns the longest possible matches when multiple keywords overlap.\n\n    Examples\n    --------\n    &gt;&gt;&gt; lexicon = Lexicon[str]()\n    &gt;&gt;&gt; lexicon[\"cat\"] = \"animal\"\n    &gt;&gt;&gt; lexicon[\"catches\"] = \"verb\"\n    &gt;&gt;&gt; text = \"The cat catches mice\"\n    &gt;&gt;&gt; for value, start, end in lexicon.extract(text):\n    ...     print(f\"Found {value} at {start}-{end}: {text[start:end]}\")\n    Found animal at 4-7: cat\n    Found verb at 8-15: catches\n    \"\"\"\n    # build the failure links if needed\n    self.build()\n    if isinstance(text, str):\n        tokens, offsets = next(self.tokeinze([text], return_offsets=True))\n        return MatchIterator(self._root, tokens, offsets)\n    return (\n        MatchIterator(self._root, tokens, offsets)\n        for tokens, offsets in self.tokeinze(text, return_offsets=True)\n    )\n</code></pre>"},{"location":"api/kb/#tklearn.kb.lexicon.Lexicon.display","title":"<code>display(node=None, depth=0)</code>","text":"<p>Display the trie structure for debugging purposes.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>Optional[TrieNode[T]]</code> <p>The node to start displaying from. If None, starts from root.</p> <code>None</code> <code>depth</code> <code>int</code> <p>Current depth in the trie for indentation.</p> <code>0</code> Notes <p>Prints a text representation of the trie structure. Useful for debugging and understanding the internal structure of the lexicon.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; lexicon = Lexicon[str]()\n&gt;&gt;&gt; lexicon[\"cat\"] = \"animal\"\n&gt;&gt;&gt; lexicon[\"catch\"] = \"verb\"\n&gt;&gt;&gt; lexicon.display()\ncat animal\ncatch verb\n</code></pre> Source code in <code>src/tklearn/kb/lexicon.py</code> <pre><code>def display(self, node: Optional[TrieNode[T]] = None, depth: int = 0):\n    \"\"\"Display the trie structure for debugging purposes.\n\n    Parameters\n    ----------\n    node : Optional[TrieNode[T]], default=None\n        The node to start displaying from. If None, starts from root.\n    depth : int, default=0\n        Current depth in the trie for indentation.\n\n    Notes\n    -----\n    Prints a text representation of the trie structure. Useful for\n    debugging and understanding the internal structure of the lexicon.\n\n    Examples\n    --------\n    &gt;&gt;&gt; lexicon = Lexicon[str]()\n    &gt;&gt;&gt; lexicon[\"cat\"] = \"animal\"\n    &gt;&gt;&gt; lexicon[\"catch\"] = \"verb\"\n    &gt;&gt;&gt; lexicon.display()\n    cat animal\n    catch verb\n    \"\"\"\n    if node is None:\n        node = self._root\n    for key, child in node.children.items():\n        if child.value is UNDEFINED:\n            print(\"  \" * depth + key, \"NULL\")\n        else:\n            print(\"  \" * depth + key, child.value)\n        self.display(child, depth + 1)\n</code></pre>"},{"location":"api/kb/#tklearn.kb.lexicon.MatchIterator","title":"<code>MatchIterator(trie, tokens, offsets)</code>","text":"<p>               Bases: <code>Generic[T]</code></p> <p>Iterator for finding longest matches in text using the Aho-Corasick algorithm.</p> <p>This class implements an iterator that finds the longest matching patterns in text using a trie data structure with failure links (Aho-Corasick algorithm). It processes tokens sequentially and returns matches along with their positions in the original text.</p> <p>Parameters:</p> Name Type Description Default <code>trie</code> <code>TrieNode[T]</code> <p>The root node of the trie containing patterns to match.</p> required <code>tokens</code> <code>list[str]</code> <p>List of tokens to search through.</p> required <code>offsets</code> <code>list[tuple[int, int]]</code> <p>List of token offsets (start, end) positions in the original text.</p> required Notes <p>The iterator implements the Aho-Corasick algorithm with modifications to find the longest matches. When multiple patterns match at a position, it returns the longest one. The algorithm uses failure links in the trie for efficient matching.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; # Create a trie with patterns\n&gt;&gt;&gt; trie = TrieNode[str]()\n&gt;&gt;&gt; # Add patterns to trie...\n&gt;&gt;&gt; tokens = [\"the\", \"quick\", \"brown\", \"fox\"]\n&gt;&gt;&gt; offsets = [(0, 3), (4, 9), (10, 15), (16, 19)]\n&gt;&gt;&gt; iterator = MatchIterator(trie, tokens, offsets)\n&gt;&gt;&gt; for value, start, end in iterator:\n...     print(f\"Found match: {value} at positions {start}-{end}\")\n</code></pre> Source code in <code>src/tklearn/kb/lexicon.py</code> <pre><code>def __init__(\n    self,\n    trie: TrieNode[T],\n    tokens: list[str],\n    offsets: list[tuple[int, int]],\n):\n    self.trie = trie\n    self.tokens = tokens\n    self.offsets = offsets\n    self.idx = 0\n    self.size = len(self.tokens)\n</code></pre>"},{"location":"api/kb/#tklearn.kb.lexicon.MatchIterator-functions","title":"Functions","text":""},{"location":"api/kb/#tklearn.kb.lexicon.MatchIterator.next","title":"<code>next()</code>","text":"<p>Get the next match in the text.</p> <p>Returns:</p> Type Description <code>Optional[tuple[T, int, int]]</code> <p>A tuple containing (value, start, end) where: - value: The value associated with the matched pattern - start: Starting position in the original text - end: Ending position in the original text Returns None if no more matches are found.</p> Notes <p>This method implements the core matching logic, finding the longest possible match at the current position using the trie structure and failure links.</p> Source code in <code>src/tklearn/kb/lexicon.py</code> <pre><code>def next(self) -&gt; Optional[tuple[T, int, int]]:\n    \"\"\"Get the next match in the text.\n\n    Returns\n    -------\n    Optional[tuple[T, int, int]]\n        A tuple containing (value, start, end) where:\n        - value: The value associated with the matched pattern\n        - start: Starting position in the original text\n        - end: Ending position in the original text\n        Returns None if no more matches are found.\n\n    Notes\n    -----\n    This method implements the core matching logic, finding the longest\n    possible match at the current position using the trie structure\n    and failure links.\n    \"\"\"\n    if self.idx &gt;= self.size:\n        raise StopIteration\n\n    longest_sequence = None\n    longest_sequence_length = 0\n    first_longest_end = 0\n    traversal_start_idx = self.idx\n    current_idx = self.idx\n\n    node = self.trie\n\n    while current_idx &lt; self.size:\n        end_token_idx = current_idx\n        token = self.tokens[current_idx]\n        current_idx += 1\n\n        if token in node.children:\n            # token found!\n            node = node.children[token]\n            if node.value is UNDEFINED:\n                pass\n            elif longest_sequence is None:\n                longest_sequence = (\n                    node.value,\n                    traversal_start_idx,\n                    end_token_idx,\n                )\n                longest_sequence_length = (\n                    end_token_idx - traversal_start_idx\n                )\n                first_longest_end = end_token_idx\n            else:\n                sequence_length = end_token_idx - traversal_start_idx\n                if sequence_length &gt; longest_sequence_length:\n                    longest_sequence = (\n                        node.value,\n                        traversal_start_idx,\n                        end_token_idx,\n                    )\n                    longest_sequence_length = sequence_length\n        elif node.fail is not None and node.fail.depth &gt; 0:\n            if end_token_idx - node.fail.depth &gt; first_longest_end:\n                break\n            # shift the start to the next possible match\n            node = node.fail\n            current_idx = end_token_idx\n            traversal_start_idx = current_idx - node.depth\n            continue\n        else:\n            # token not found in the node\n            break\n\n    self.idx = traversal_start_idx + 1\n\n    if longest_sequence is None:\n        return\n\n    # self.idx = longest_sequence[2] + 1\n\n    return (\n        longest_sequence[0],\n        self.offsets[longest_sequence[1]][0],\n        self.offsets[longest_sequence[2]][1],\n    )\n</code></pre>"},{"location":"api/kb/#tklearn.kb.lexicon.MatchIterator.__next__","title":"<code>__next__()</code>","text":"<p>Get the next match, implementing the iterator protocol.</p> <p>Returns:</p> Type Description <code>tuple[T, int, int]</code> <p>A tuple containing (value, start, end) for the next match. - value: The value associated with the matched pattern - start: Starting position in the original text - end: Ending position in the original text</p> <p>Raises:</p> Type Description <code>StopIteration</code> <p>When no more matches are found.</p> Source code in <code>src/tklearn/kb/lexicon.py</code> <pre><code>def __next__(self) -&gt; tuple[T, int, int]:\n    \"\"\"Get the next match, implementing the iterator protocol.\n\n    Returns\n    -------\n    tuple[T, int, int]\n        A tuple containing (value, start, end) for the next match.\n        - value: The value associated with the matched pattern\n        - start: Starting position in the original text\n        - end: Ending position in the original text\n\n    Raises\n    ------\n    StopIteration\n        When no more matches are found.\n    \"\"\"\n    result = self.next()\n    while result is None:\n        result = self.next()\n    return result\n</code></pre>"},{"location":"api/kb/#tklearn.kb.lexicon.MatchIterator.__iter__","title":"<code>__iter__()</code>","text":"<p>Make the class iterable.</p> <p>Returns:</p> Type Description <code>MatchIterator[T]</code> <p>Returns self to implement the iterator protocol.</p> Source code in <code>src/tklearn/kb/lexicon.py</code> <pre><code>def __iter__(self) -&gt; MatchIterator[T]:\n    \"\"\"Make the class iterable.\n\n    Returns\n    -------\n    MatchIterator[T]\n        Returns self to implement the iterator protocol.\n    \"\"\"\n    return self\n</code></pre>"},{"location":"api/kb/#tklearn.kb.lexicon.MatchIterator.__len__","title":"<code>__len__()</code>","text":"<p>Get the total number of tokens in the input text.</p> <p>Returns:</p> Type Description <code>int</code> <p>The total number of tokens in the input text that this iterator is searching through. This represents the size of the search space, not the number of matches that will be found.</p> Source code in <code>src/tklearn/kb/lexicon.py</code> <pre><code>def __len__(self):\n    \"\"\"Get the total number of tokens in the input text.\n\n    Returns\n    -------\n    int\n        The total number of tokens in the input text that this iterator\n        is searching through. This represents the size of the search space,\n        not the number of matches that will be found.\n    \"\"\"\n    return len(self.tokens)\n</code></pre>"},{"location":"api/kb/#models","title":"Models","text":""},{"location":"api/kb/#tklearn.kb.models","title":"<code>models</code>","text":""},{"location":"api/kb/#triple-store","title":"Triple Store","text":""},{"location":"api/kb/#tklearn.kb.triple_store","title":"<code>triple_store</code>","text":""},{"location":"api/kb/#tklearn.kb.triple_store-classes","title":"Classes","text":""},{"location":"api/kb/#tklearn.kb.triple_store.TripleStore","title":"<code>TripleStore(path=':memory:', read_only=False)</code>","text":"Source code in <code>src/tklearn/kb/triple_store.py</code> <pre><code>def __init__(self, path: str | Path = \":memory:\", read_only: bool = False):\n    self.path = path\n    self.read_only = read_only\n    self.con = duckdb.connect(path, read_only=read_only)\n    if read_only:\n        return\n    self.create_table()\n</code></pre>"},{"location":"api/kb/#tklearn.kb.triple_store.TripleStore-functions","title":"Functions","text":""},{"location":"api/kb/#tklearn.kb.triple_store.TripleStore.get","title":"<code>get(subject, default=None)</code>","text":"<p>Return all triples in the store.</p> Source code in <code>src/tklearn/kb/triple_store.py</code> <pre><code>def get(\n    self, subject: str | tuple[str, int], default: T = None\n) -&gt; dict[str, set[tuple[str, int]]] | T:\n    \"\"\"Return all triples in the store.\"\"\"\n    if getattr(self, \"_triples\", None) is None:\n        self._triples = self._build_triples_cache()\n    if isinstance(subject, str):\n        subj_word, subj_sense = subject, None\n    else:\n        subj_word, subj_sense = subject\n    # subj_word = subj_word.lower()\n    senses = self._triples.get(subj_word)\n    if senses is None:\n        return default\n    if subj_sense is None:\n        relations = defaultdict(set)\n        for sense_dict in senses.values():\n            for predicate, objects in sense_dict.items():\n                relations[predicate].update(objects)\n    elif subj_sense in senses:\n        relations = defaultdict(set)\n        for predicate, objects in senses[subj_sense].items():\n            relations[predicate].update(objects)\n    else:\n        return default\n    return dict(relations)\n</code></pre>"},{"location":"api/kb/#wiktionary","title":"Wiktionary","text":""},{"location":"api/kb/#models_1","title":"Models","text":""},{"location":"api/kb/#tklearn.kb.wiktionary.models","title":"<code>models</code>","text":""},{"location":"api/kb/#tklearn.kb.wiktionary.models-functions","title":"Functions","text":""},{"location":"api/kb/#tklearn.kb.wiktionary.models.parse_jsonl","title":"<code>parse_jsonl(file_path)</code>","text":"<p>Parses a JSONL file and yields validated Word objects.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str</code> <p>Path to the JSONL file.</p> required <p>Yields:</p> Type Description <code>Word</code> <p>Parsed and validated Word object.</p> Source code in <code>src/tklearn/kb/wiktionary/models.py</code> <pre><code>def parse_jsonl(file_path: str) -&gt; Iterator[Word]:\n    \"\"\"\n    Parses a JSONL file and yields validated Word objects.\n\n    Parameters\n    ----------\n    file_path : str\n        Path to the JSONL file.\n\n    Yields\n    ------\n    Word\n        Parsed and validated Word object.\n    \"\"\"\n    with open(file_path, \"r\", encoding=\"utf-8\") as file:\n        for line in file:\n            line = line.strip()\n            if not line:\n                continue\n            data = orjson.loads(line)\n            yield Word.model_validate(data)\n</code></pre>"},{"location":"api/kb/#store","title":"Store","text":""},{"location":"api/kb/#tklearn.kb.wiktionary.store","title":"<code>store</code>","text":""},{"location":"api/kb/#tklearn.kb.wiktionary.store-classes","title":"Classes","text":""},{"location":"api/kb/#tklearn.kb.wiktionary.store.WiktionaryArtifactStoreV1","title":"<code>WiktionaryArtifactStoreV1</code>","text":"<p>               Bases: <code>ArtifactStore</code></p>"},{"location":"api/kb/#tklearn.kb.wiktionary.store.WiktionaryArtifactStoreV1-functions","title":"Functions","text":""},{"location":"api/kb/#tklearn.kb.wiktionary.store.WiktionaryArtifactStoreV1.setup_lexicon","title":"<code>setup_lexicon()</code>","text":"<p>Returns the lexicon of forms.</p> Source code in <code>src/tklearn/kb/wiktionary/store.py</code> <pre><code>def setup_lexicon(self) -&gt; Lexicon[set[str]]:\n    \"\"\"Returns the lexicon of forms.\"\"\"\n    try:\n        return Lexicon.load(self.forms_lexicon_path)\n    except FileNotFoundError:\n        pass\n    lexicon = Lexicon()\n    progress_bar = tqdm.tqdm(\n        total=self.wiktionary_size, desc=\"Adding forms\", unit=\"word\"\n    )\n    for word in parse_jsonl(self.wiktionary_path):\n        if any([\n            self.lang_code == (word.lang_code or \"\").lower(),\n            self.lang_code == (word.lang or \"\").lower(),\n        ]):\n            add_forms(word, lexicon=lexicon)\n        progress_bar.update(1)\n    progress_bar.close()\n    lexicon.dump(self.forms_lexicon_path)\n    return lexicon\n</code></pre>"},{"location":"api/kb/#tklearn.kb.wiktionary.store-functions","title":"Functions","text":""},{"location":"api/kb/#tklearn.kb.wiktionary.store.add_forms","title":"<code>add_forms(w, lexicon)</code>","text":"<p>Stores form mappings in LevelDB using batch writing.</p> Source code in <code>src/tklearn/kb/wiktionary/store.py</code> <pre><code>def add_forms(w: Word, lexicon: Lexicon):\n    \"\"\"Stores form mappings in LevelDB using batch writing.\"\"\"\n    word = (w.word or \"\").strip()\n    if not word:\n        return\n    base_forms: set = lexicon.get(word, {word})\n    for form_of in w.form_of or []:\n        form_of_word = (form_of.word or \"\").strip()\n        base_forms.add(form_of_word)\n    lexicon[word] = base_forms\n    for form in w.forms or []:\n        form_form = (form.form or \"\").strip()\n        if not form_form:\n            continue\n        base_forms: set = lexicon.get(form_form, set())\n        base_forms.add(word)\n        lexicon[form_form] = base_forms\n</code></pre>"},{"location":"api/metrics/","title":"Metrics","text":"<p>Evaluation metrics for text classification and other NLP tasks.</p>"},{"location":"api/metrics/#base-classes","title":"Base Classes","text":""},{"location":"api/metrics/#tklearn.metrics.base","title":"<code>base</code>","text":""},{"location":"api/metrics/#classification-metrics","title":"Classification Metrics","text":""},{"location":"api/metrics/#tklearn.metrics.classification","title":"<code>classification</code>","text":""},{"location":"api/metrics/#helpers","title":"Helpers","text":""},{"location":"api/metrics/#tklearn.metrics.helpers","title":"<code>helpers</code>","text":""},{"location":"api/nn/","title":"Neural Networks","text":"<p>Neural network models, training utilities, and optimization tools for text processing tasks.</p>"},{"location":"api/nn/#core-modules","title":"Core Modules","text":""},{"location":"api/nn/#loss-functions","title":"Loss Functions","text":""},{"location":"api/nn/#tklearn.nn.loss","title":"<code>loss</code>","text":""},{"location":"api/nn/#tklearn.nn.loss-classes","title":"Classes","text":""},{"location":"api/nn/#optimizers","title":"Optimizers","text":""},{"location":"api/nn/#tklearn.nn.optim","title":"<code>optim</code>","text":""},{"location":"api/nn/#base-classes","title":"Base Classes","text":""},{"location":"api/nn/#encoder","title":"Encoder","text":""},{"location":"api/nn/#tklearn.nn.base.encoder","title":"<code>encoder</code>","text":""},{"location":"api/nn/#tklearn.nn.base.encoder-classes","title":"Classes","text":""},{"location":"api/nn/#tklearn.nn.base.encoder.Encoder","title":"<code>Encoder(model, dataloader, callbacks=None)</code>","text":"<p>               Bases: <code>CallbacksMixin</code>, <code>Generic[K, V]</code></p> <p>A class for encoding data using a model and dataloader, with support for callbacks.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module[K, V]</code> <p>The model used for encoding.</p> required <code>dataloader</code> <code>DataLoader</code> <p>The dataloader providing batches of data to encode.</p> required <code>callbacks</code> <code>CallbackList | Iterable[Callback] | None</code> <p>A list or iterable of callbacks to use during encoding, by default None.</p> <code>None</code> <p>Methods:</p> Name Description <code>iter_batches</code> <p>Iterates over batches of data, yielding batch index, batch data, model output, and loss dictionary.</p> <code>encode</code> <p>Encodes the data and returns the encodings in the specified format.</p> Notes <ul> <li>The <code>iter_batches</code> method ensures the model is in evaluation mode during batch iteration.</li> <li>The <code>encode</code> method supports multiple output formats, including PyTorch tensors, NumPy arrays, and Python lists.</li> </ul> Source code in <code>src/tklearn/nn/base/encoder.py</code> <pre><code>def __init__(\n    self,\n    model: Module[K, V],\n    dataloader: DataLoader,\n    callbacks: CallbackList | Iterable[Callback] | None = None,\n) -&gt; None:\n    super().__init__()\n    self.model = model\n    self.dataloader = dataloader\n    self.callbacks = callbacks\n</code></pre>"},{"location":"api/nn/#tklearn.nn.base.encoder-functions","title":"Functions","text":""},{"location":"api/nn/#tklearn.nn.base.encoder.encode","title":"<code>encode(dataset, model, batch_size=DEFAULT_BATCH_SIZE, pin_memory=True, desc='Encoding dataset', encode_batch_size=32, collate_fn=None, output_column_name=None, **kwargs)</code>","text":"<p>Encodes a dataset using a given model.</p> <p>This function processes a dataset in chunks, encoding each chunk using the provided model. It supports batching, custom collation functions, and other configurations to optimize the encoding process.</p> <p>Parameters:</p> Name Type Description Default <code>dataset</code> <code>Dataset</code> <p>The dataset to be encoded.</p> required <code>model</code> <code>Module</code> <p>The model used for encoding the dataset.</p> required <code>batch_size</code> <code>int</code> <p>The size of the batches to process the dataset, by default DEFAULT_BATCH_SIZE.</p> <code>DEFAULT_BATCH_SIZE</code> <code>pin_memory</code> <code>bool</code> <p>If True, the data loader will copy tensors into CUDA pinned memory, by default True.</p> <code>True</code> <code>desc</code> <code>str</code> <p>A description for the progress bar, by default \"Encoding dataset\".</p> <code>'Encoding dataset'</code> <code>encode_batch_size</code> <code>int</code> <p>The batch size used for encoding within each chunk, by default 32.</p> <code>32</code> <code>collate_fn</code> <code>Callable or None</code> <p>A function to merge a list of samples into a batch, by default None.</p> <code>None</code> <code>output_column_name</code> <code>str</code> <p>The name of the column to store the encoded embeddings, by default \"embedding\".</p> <code>None</code> <code>**kwargs</code> <p>Additional keyword arguments passed to the dataset's <code>map</code> function.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Dataset</code> <p>The encoded dataset.</p> Source code in <code>src/tklearn/nn/base/encoder.py</code> <pre><code>def encode(\n    dataset: Dataset,\n    model: Module,\n    batch_size: int = DEFAULT_BATCH_SIZE,\n    pin_memory: bool = True,\n    desc: str = \"Encoding dataset\",\n    encode_batch_size: int = 32,\n    collate_fn: Callable | None = None,\n    output_column_name: str | None = None,\n    **kwargs,\n) -&gt; Dataset:\n    \"\"\"\n    Encodes a dataset using a given model.\n\n    This function processes a dataset in chunks, encoding each chunk using the\n    provided model. It supports batching, custom collation functions, and other\n    configurations to optimize the encoding process.\n\n    Parameters\n    ----------\n    dataset : Dataset\n        The dataset to be encoded.\n    model : Module\n        The model used for encoding the dataset.\n    batch_size : int, optional\n        The size of the batches to process the dataset, by default DEFAULT_BATCH_SIZE.\n    pin_memory : bool, optional\n        If True, the data loader will copy tensors into CUDA pinned memory, by default True.\n    desc : str, optional\n        A description for the progress bar, by default \"Encoding dataset\".\n    encode_batch_size : int, optional\n        The batch size used for encoding within each chunk, by default 32.\n    collate_fn : Callable or None, optional\n        A function to merge a list of samples into a batch, by default None.\n    output_column_name : str, optional\n        The name of the column to store the encoded embeddings, by default \"embedding\".\n    **kwargs\n        Additional keyword arguments passed to the dataset's `map` function.\n\n    Returns\n    -------\n    Dataset\n        The encoded dataset.\n    \"\"\"\n    if collate_fn is not None:\n        kwargs[\"collate_fn\"] = collate_fn\n    if output_column_name is None:\n        output_column_name = \"embedding\"\n    model.eval()\n    encoded_dataset = dataset.map(\n        partial(\n            _encode_chunk,\n            model=model,\n            batch_size=encode_batch_size,\n            pin_memory=pin_memory,\n            output_column_name=output_column_name,\n            **kwargs,\n        ),\n        batched=True,\n        batch_size=batch_size,\n        desc=desc,\n        with_indices=True,\n    )\n    return encoded_dataset\n</code></pre>"},{"location":"api/nn/#evaluator","title":"Evaluator","text":""},{"location":"api/nn/#tklearn.nn.base.evaluator","title":"<code>evaluator</code>","text":""},{"location":"api/nn/#tklearn.nn.base.evaluator-classes","title":"Classes","text":""},{"location":"api/nn/#tklearn.nn.base.evaluator-functions","title":"Functions","text":""},{"location":"api/nn/#module","title":"Module","text":""},{"location":"api/nn/#tklearn.nn.base.module","title":"<code>module</code>","text":""},{"location":"api/nn/#tklearn.nn.base.module-classes","title":"Classes","text":""},{"location":"api/nn/#tklearn.nn.base.module.Module","title":"<code>Module</code>","text":"<p>               Bases: <code>Module</code>, <code>Generic[K, V]</code></p>"},{"location":"api/nn/#tklearn.nn.base.module.Module-functions","title":"Functions","text":""},{"location":"api/nn/#tklearn.nn.base.module.Module.freeze_layers","title":"<code>freeze_layers(layers=None, prefix='')</code>","text":"<p>Freeze layers in the model that match the given patterns.</p> <p>Parameters:</p> Name Type Description Default <code>layers</code> <code>list of str</code> <p>A list of layer names or patterns to freeze. Supports wildcards (*) and dot notation for nested layers. If None, no layers will be frozen.</p> <code>None</code> <code>prefix</code> <code>str</code> <p>An optional prefix to apply to all layer patterns.</p> <code>\"\"</code> <p>Returns:</p> Type Description <code>int</code> <p>The number of parameters frozen.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If an invalid regex pattern is provided.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; model.freeze_layers(['encoder.*', 'encoder.layer.[0-8].*'])\n&gt;&gt;&gt; model.freeze_layers(['layer_[1-3]'], prefix='transformer')\n</code></pre> Notes <p>This method uses regular expressions to match layer names. Dots in layer names are treated as literal dots, while asterisks are treated as wildcards.</p> Source code in <code>src/tklearn/nn/base/module.py</code> <pre><code>def freeze_layers(\n    self, layers: Optional[List[str]] = None, prefix: str = \"\"\n) -&gt; int:\n    \"\"\"\n    Freeze layers in the model that match the given patterns.\n\n    Parameters\n    ----------\n    layers : list of str, optional\n        A list of layer names or patterns to freeze. Supports wildcards (*)\n        and dot notation for nested layers. If None, no layers will be frozen.\n    prefix : str, default=\"\"\n        An optional prefix to apply to all layer patterns.\n\n    Returns\n    -------\n    int\n        The number of parameters frozen.\n\n    Raises\n    ------\n    ValueError\n        If an invalid regex pattern is provided.\n\n    Examples\n    --------\n    &gt;&gt;&gt; model.freeze_layers(['encoder.*', 'encoder.layer.[0-8].*'])\n    &gt;&gt;&gt; model.freeze_layers(['layer_[1-3]'], prefix='transformer')\n\n    Notes\n    -----\n    This method uses regular expressions to match layer names. Dots in layer\n    names are treated as literal dots, while asterisks are treated as wildcards.\n    \"\"\"\n    if not layers:\n        return 0  # no layers to freeze\n    # escape dots and convert asterisks to regex wildcards\n    layers = [p.replace(\".\", r\"\\.\").replace(\"*\", \".*\") for p in layers]\n    pattern_regex = \"|\".join(layers)\n    if prefix:\n        pattern_regex = rf\"{prefix}\\.({pattern_regex})\"\n    # compile regex pattern\n    try:\n        pattern = re.compile(f\"^{pattern_regex}$\")\n    except re.error as e:\n        raise ValueError(str(e))\n    # freeze parameters that match the pattern\n    frozen_params = 0\n    for name, param in self.named_parameters():\n        if not pattern.match(name):\n            continue\n        param.requires_grad = False\n        frozen_params += param.numel()\n    # return number of frozen parameters\n    return frozen_params\n</code></pre>"},{"location":"api/nn/#predictor","title":"Predictor","text":""},{"location":"api/nn/#tklearn.nn.base.predictor","title":"<code>predictor</code>","text":""},{"location":"api/nn/#tklearn.nn.base.predictor-classes","title":"Classes","text":""},{"location":"api/nn/#tklearn.nn.base.predictor-functions","title":"Functions","text":""},{"location":"api/nn/#trainer","title":"Trainer","text":""},{"location":"api/nn/#tklearn.nn.base.trainer","title":"<code>trainer</code>","text":""},{"location":"api/nn/#tklearn.nn.base.trainer-classes","title":"Classes","text":""},{"location":"api/nn/#tklearn.nn.base.trainer-functions","title":"Functions","text":""},{"location":"api/nn/#calibration","title":"Calibration","text":""},{"location":"api/nn/#temperature-scaling","title":"Temperature Scaling","text":""},{"location":"api/nn/#tklearn.nn.calibration.temperature","title":"<code>temperature</code>","text":""},{"location":"api/nn/#tklearn.nn.calibration.temperature-classes","title":"Classes","text":""},{"location":"api/nn/#tklearn.nn.calibration.temperature.TemperatureTrainer","title":"<code>TemperatureTrainer(model, dataloader, max_iters=50, device=None)</code>","text":"Source code in <code>src/tklearn/nn/calibration/temperature.py</code> <pre><code>def __init__(\n    self,\n    model: Module,\n    dataloader: DataLoader,\n    max_iters: int = 50,\n    device: Optional[str] = None,\n):\n    self.base_model = model\n    self.temperature_model = TemperatureScaling()\n    self.validation_loader = dataloader\n    self.max_iters = max_iters\n    self.device = device or model.device\n    # Move models to device\n    self.base_model.to(self.device)\n    self.temperature_model.to(self.device)\n</code></pre>"},{"location":"api/nn/#tklearn.nn.calibration.temperature.TemperatureTrainer-functions","title":"Functions","text":""},{"location":"api/nn/#tklearn.nn.calibration.temperature.TemperatureTrainer.collect_logits","title":"<code>collect_logits()</code>","text":"<p>Collect all logits and labels from validation set.</p> Source code in <code>src/tklearn/nn/calibration/temperature.py</code> <pre><code>def collect_logits(self) -&gt; tuple[torch.Tensor, torch.Tensor]:\n    \"\"\"Collect all logits and labels from validation set.\"\"\"\n    self.base_model.eval()\n    logits_list = []\n    labels_list = []\n    with torch.no_grad():\n        for b, batch in enumerate(self.validation_loader):\n            batch = move_to_device(batch, self.device, non_blocking=True)\n            output = self.base_model.predict_step(batch, batch_idx=b)\n            labels, logits = batch[\"labels\"], output[\"logits\"]\n            logits_list.append(logits)\n            labels_list.append(labels)\n        logits = torch.cat(logits_list)\n        labels = torch.cat(labels_list)\n    return logits, labels\n</code></pre>"},{"location":"api/nn/#tklearn.nn.calibration.temperature.TemperatureTrainer.train","title":"<code>train()</code>","text":"<p>Train the temperature scaling model.</p> Source code in <code>src/tklearn/nn/calibration/temperature.py</code> <pre><code>def train(self) -&gt; Dict[str, Any]:\n    \"\"\"Train the temperature scaling model.\"\"\"\n    logits, labels = self.collect_logits()\n\n    # Switch to Adam optimizer\n    optimizer = torch.optim.Adam(\n        self.temperature_model.parameters(), lr=0.01\n    )\n\n    # Track initial temperature\n    initial_temp = self.temperature_model.temperature.item()\n\n    # Training loop\n    for i in range(self.max_iters):\n        optimizer.zero_grad()\n        scaled_logits = self.temperature_model(logits)\n        loss = self.base_model.compute_loss(\n            {\"labels\": labels}, {\"logits\": scaled_logits}\n        )\n        loss.backward()\n        optimizer.step()\n\n    # Return training info\n    final_temp = self.temperature_model.temperature.item()\n    final_loss = loss.item()\n\n    return {\n        \"temperature\": final_temp,\n        \"final_nll\": final_loss,\n        \"initial_temp\": initial_temp,\n    }\n</code></pre>"},{"location":"api/nn/#tklearn.nn.calibration.temperature.CalibratedModule","title":"<code>CalibratedModule(base_model, temperature_model, info)</code>","text":"<p>               Bases: <code>Module</code></p> <p>Wrapper for a model with temperature scaling calibration.</p> Source code in <code>src/tklearn/nn/calibration/temperature.py</code> <pre><code>def __init__(\n    self,\n    base_model: LinearMulticlassClassifier,\n    temperature_model: TemperatureScaling,\n    info: Dict[str, Any],\n):\n    super().__init__()\n    self.base_model = base_model\n    self.temperature_model = temperature_model\n    self.info = info\n</code></pre>"},{"location":"api/nn/#tklearn.nn.calibration.temperature-functions","title":"Functions","text":""},{"location":"api/nn/#tklearn.nn.calibration.temperature.calibrate_model","title":"<code>calibrate_model(model, dataloader, max_iters=50, device=None)</code>","text":"<p>Calibrate a model using temperature scaling.</p> <p>Returns:</p> Type Description <code>tuple</code> <p>A tuple containing the calibrated model and calibration info.</p> Source code in <code>src/tklearn/nn/calibration/temperature.py</code> <pre><code>def calibrate_model(\n    model: Module,\n    dataloader: DataLoader,\n    max_iters: int = 50,\n    device: Optional[str] = None,\n) -&gt; CalibratedModule:\n    \"\"\"\n    Calibrate a model using temperature scaling.\n\n    Returns\n    -------\n    tuple\n        A tuple containing the calibrated model and calibration info.\n    \"\"\"\n    trainer = TemperatureTrainer(\n        model=model,\n        dataloader=dataloader,\n        max_iters=max_iters,\n        device=device,\n    )\n    info = trainer.train()\n    calibrated_model = CalibratedModule(model, trainer.temperature_model, info)\n    return calibrated_model\n</code></pre>"},{"location":"api/nn/#callbacks","title":"Callbacks","text":""},{"location":"api/nn/#base-callback","title":"Base Callback","text":""},{"location":"api/nn/#tklearn.nn.callbacks.base","title":"<code>base</code>","text":""},{"location":"api/nn/#tklearn.nn.callbacks.base-classes","title":"Classes","text":""},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback","title":"<code>Callback(*args, **kwargs)</code>","text":"Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def __init__(self, *args, **kwargs) -&gt; None:\n    self._model = None\n    self._params = {}\n    super().__init__(*args, **kwargs)\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback-attributes","title":"Attributes","text":""},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.model","title":"<code>model</code>  <code>property</code>","text":"<p>Get the model associated with the callback.</p> <p>Returns:</p> Type Description <code>Model</code> <p>The model associated with the callback.</p>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.params","title":"<code>params</code>  <code>property</code>","text":"<p>Get the parameters of the callback.</p> <p>Returns:</p> Type Description <code>dict</code> <p>The parameters of the callback.</p>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback-functions","title":"Functions","text":""},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.set_model","title":"<code>set_model(model)</code>","text":"<p>Set the model associated with the callback.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Model</code> <p>The model to be set.</p> required Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def set_model(self, model: Module):\n    \"\"\"\n    Set the model associated with the callback.\n\n    Parameters\n    ----------\n    model : Model\n        The model to be set.\n    \"\"\"\n    if (\n        model is not None\n        and self._model is not None\n        and model is not self._model\n    ):\n        msg = (\n            f\"the callback '{type(self).__name__}' is already \"\n            f\"associated with a model '{type(self._model).__name__}'\"\n        )\n        raise ValueError(msg)\n    self._model = model\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.set_params","title":"<code>set_params(params)</code>","text":"<p>Set the parameters of the callback.</p> <p>Parameters:</p> Name Type Description Default <code>params</code> <code>dict</code> <p>The parameters to be set.</p> required Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def set_params(self, params):\n    \"\"\"\n    Set the parameters of the callback.\n\n    Parameters\n    ----------\n    params : dict\n        The parameters to be set.\n    \"\"\"\n    if params is None:\n        params = {}\n    self._params = params\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_epoch_begin","title":"<code>on_epoch_begin(epoch, logs=None)</code>","text":"<p>Called at the start of an epoch.</p> <p>Parameters:</p> Name Type Description Default <code>epoch</code> <code>int</code> <p>Index of epoch.</p> required <code>logs</code> <code>dict</code> <p>Currently no data is passed to this argument for this method but that may change in the future.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_epoch_begin(self, epoch, logs=None):\n    \"\"\"\n    Called at the start of an epoch.\n\n    Parameters\n    ----------\n    epoch : int\n        Index of epoch.\n    logs : dict, optional\n        Currently no data is passed to this argument for this method but\n        that may change in the future.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_epoch_end","title":"<code>on_epoch_end(epoch, logs=None)</code>","text":"<p>Called at the end of an epoch.</p> <p>Subclasses should override for any actions to run. This function should only be called during TRAIN mode.</p> <p>Parameters:</p> Name Type Description Default <code>epoch</code> <code>int</code> <p>Index of epoch.</p> required <code>logs</code> <code>dict</code> <p>Metric results for this training epoch, and for the validation epoch if validation is performed. Validation result keys are prefixed with <code>val_</code>. For the training epoch, the values of the <code>Model</code>'s metrics are returned. Example: <code>{'loss': 0.2, 'accuracy': 0.7}</code>.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_epoch_end(self, epoch, logs=None):\n    \"\"\"\n    Called at the end of an epoch.\n\n    Subclasses should override for any actions to run. This function should\n    only be called during TRAIN mode.\n\n    Parameters\n    ----------\n    epoch : int\n        Index of epoch.\n    logs : dict, optional\n        Metric results for this training epoch, and for the validation\n        epoch if validation is performed.\n        Validation result keys are prefixed with `val_`.\n        For the training epoch, the values of the `Model`'s metrics are\n        returned.\n        Example: `{'loss': 0.2, 'accuracy': 0.7}`.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_train_batch_begin","title":"<code>on_train_batch_begin(batch, logs=None)</code>","text":"<p>Called at the beginning of a training batch in <code>fit</code> methods.</p> <p>Subclasses should override for any actions to run.</p> <p>Note that if the <code>steps_per_execution</code> argument to <code>compile</code> in <code>Model</code> is set to <code>N</code>, this method will only be called every <code>N</code> batches.</p> <p>Parameters:</p> Name Type Description Default <code>batch</code> <code>int</code> <p>Index of batch within the current epoch.</p> required <code>logs</code> <code>dict</code> <p>Currently no data is passed to this argument for this method but that may change in the future.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_train_batch_begin(self, batch, logs=None):\n    \"\"\"\n    Called at the beginning of a training batch in `fit` methods.\n\n    Subclasses should override for any actions to run.\n\n    Note that if the `steps_per_execution` argument to `compile` in\n    `Model` is set to `N`, this method will only be called every\n    `N` batches.\n\n    Parameters\n    ----------\n    batch : int\n        Index of batch within the current epoch.\n    logs : dict, optional\n        Currently no data is passed to this argument for this method\n        but that may change in the future.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_train_batch_end","title":"<code>on_train_batch_end(batch, logs=None)</code>","text":"<p>Called at the end of a training batch in <code>fit</code> methods.</p> <p>Subclasses should override for any actions to run.</p> <p>Note that if the <code>steps_per_execution</code> argument to <code>compile</code> in <code>Model</code> is set to <code>N</code>, this method will only be called every <code>N</code> batches.</p> <p>Parameters:</p> Name Type Description Default <code>batch</code> <code>int</code> <p>Index of batch within the current epoch.</p> required <code>logs</code> <code>dict</code> <p>Aggregated metric results up until this batch.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_train_batch_end(self, batch, logs=None):\n    \"\"\"\n    Called at the end of a training batch in `fit` methods.\n\n    Subclasses should override for any actions to run.\n\n    Note that if the `steps_per_execution` argument to `compile` in\n    `Model` is set to `N`, this method will only be called every\n    `N` batches.\n\n    Parameters\n    ----------\n    batch : int\n        Index of batch within the current epoch.\n    logs : dict, optional\n        Aggregated metric results up until this batch.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_test_batch_begin","title":"<code>on_test_batch_begin(batch, logs=None)</code>","text":"<p>Called at the beginning of a batch in <code>evaluate</code> methods.</p> <p>Also called at the beginning of a validation batch in the <code>fit</code> methods, if validation data is provided.</p> <p>Subclasses should override for any actions to run.</p> <p>Note that if the <code>steps_per_execution</code> argument to <code>compile</code> in <code>Model</code> is set to <code>N</code>, this method will only be called every <code>N</code> batches.</p> <p>Parameters:</p> Name Type Description Default <code>batch</code> <code>int</code> <p>Index of batch within the current epoch.</p> required <code>logs</code> <code>dict</code> <p>Currently no data is passed to this argument for this method but that may change in the future.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_test_batch_begin(self, batch, logs=None):\n    \"\"\"\n    Called at the beginning of a batch in `evaluate` methods.\n\n    Also called at the beginning of a validation batch in the `fit`\n    methods, if validation data is provided.\n\n    Subclasses should override for any actions to run.\n\n    Note that if the `steps_per_execution` argument to `compile` in\n    `Model` is set to `N`, this method will only be called every\n    `N` batches.\n\n    Parameters\n    ----------\n    batch : int\n        Index of batch within the current epoch.\n    logs : dict, optional\n        Currently no data is passed to this argument for this method\n        but that may change in the future.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_test_batch_end","title":"<code>on_test_batch_end(batch, logs=None)</code>","text":"<p>Called at the end of a batch in <code>evaluate</code> methods.</p> <p>Also called at the end of a validation batch in the <code>fit</code> methods, if validation data is provided.</p> <p>Subclasses should override for any actions to run.</p> <p>Note that if the <code>steps_per_execution</code> argument to <code>compile</code> in <code>Model</code> is set to <code>N</code>, this method will only be called every <code>N</code> batches.</p> <p>Parameters:</p> Name Type Description Default <code>batch</code> <code>int</code> <p>Index of batch within the current epoch.</p> required <code>logs</code> <code>dict</code> <p>Aggregated metric results up until this batch.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_test_batch_end(self, batch, logs=None):\n    \"\"\"\n    Called at the end of a batch in `evaluate` methods.\n\n    Also called at the end of a validation batch in the `fit`\n    methods, if validation data is provided.\n\n    Subclasses should override for any actions to run.\n\n    Note that if the `steps_per_execution` argument to `compile` in\n    `Model` is set to `N`, this method will only be called every\n    `N` batches.\n\n    Parameters\n    ----------\n    batch : int\n        Index of batch within the current epoch.\n    logs : dict, optional\n        Aggregated metric results up until this batch.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_predict_batch_begin","title":"<code>on_predict_batch_begin(batch, logs=None)</code>","text":"<p>Called at the beginning of a batch in <code>predict</code> methods.</p> <p>Subclasses should override for any actions to run.</p> <p>Note that if the <code>steps_per_execution</code> argument to <code>compile</code> in <code>Model</code> is set to <code>N</code>, this method will only be called every <code>N</code> batches.</p> <p>Parameters:</p> Name Type Description Default <code>batch</code> <code>int</code> <p>Index of batch within the current epoch.</p> required <code>logs</code> <code>dict</code> <p>Currently no data is passed to this argument for this method but that may change in the future.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_predict_batch_begin(self, batch, logs=None):\n    \"\"\"\n    Called at the beginning of a batch in `predict` methods.\n\n    Subclasses should override for any actions to run.\n\n    Note that if the `steps_per_execution` argument to `compile` in\n    `Model` is set to `N`, this method will only be called every\n    `N` batches.\n\n    Parameters\n    ----------\n    batch : int\n        Index of batch within the current epoch.\n    logs : dict, optional\n        Currently no data is passed to this argument for this method\n        but that may change in the future.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_predict_batch_end","title":"<code>on_predict_batch_end(batch, logs=None)</code>","text":"<p>Called at the end of a batch in <code>predict</code> methods.</p> <p>Subclasses should override for any actions to run.</p> <p>Note that if the <code>steps_per_execution</code> argument to <code>compile</code> in <code>Model</code> is set to <code>N</code>, this method will only be called every <code>N</code> batches.</p> <p>Parameters:</p> Name Type Description Default <code>batch</code> <code>int</code> <p>Index of batch within the current epoch.</p> required <code>logs</code> <code>dict</code> <p>Aggregated metric results up until this batch.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_predict_batch_end(self, batch, logs=None):\n    \"\"\"\n    Called at the end of a batch in `predict` methods.\n\n    Subclasses should override for any actions to run.\n\n    Note that if the `steps_per_execution` argument to `compile` in\n    `Model` is set to `N`, this method will only be called every\n    `N` batches.\n\n    Parameters\n    ----------\n    batch : int\n        Index of batch within the current epoch.\n    logs : dict, optional\n        Aggregated metric results up until this batch.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_train_begin","title":"<code>on_train_begin(logs=None)</code>","text":"<p>Called at the beginning of training.</p> <p>Subclasses should override for any actions to run.</p> <p>Parameters:</p> Name Type Description Default <code>logs</code> <code>dict</code> <p>Currently no data is passed to this argument for this method but that may change in the future.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_train_begin(self, logs=None):\n    \"\"\"\n    Called at the beginning of training.\n\n    Subclasses should override for any actions to run.\n\n    Parameters\n    ----------\n    logs : dict, optional\n        Currently no data is passed to this argument for this method\n        but that may change in the future.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_train_end","title":"<code>on_train_end(logs=None)</code>","text":"<p>Called at the end of training.</p> <p>Subclasses should override for any actions to run.</p> <p>Parameters:</p> Name Type Description Default <code>logs</code> <code>dict</code> <p>Currently the output of the last call to <code>on_epoch_end()</code> is passed to this argument for this method but that may change in the future.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_train_end(self, logs=None):\n    \"\"\"\n    Called at the end of training.\n\n    Subclasses should override for any actions to run.\n\n    Parameters\n    ----------\n    logs : dict, optional\n        Currently the output of the last call to `on_epoch_end()` is\n        passed to this argument for this method but that may change\n        in the future.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_test_begin","title":"<code>on_test_begin(logs=None)</code>","text":"<p>Called at the beginning of evaluation or validation.</p> <p>Subclasses should override for any actions to run.</p> <p>Parameters:</p> Name Type Description Default <code>logs</code> <code>dict</code> <p>Currently no data is passed to this argument for this method but that may change in the future.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_test_begin(self, logs=None):\n    \"\"\"\n    Called at the beginning of evaluation or validation.\n\n    Subclasses should override for any actions to run.\n\n    Parameters\n    ----------\n    logs : dict, optional\n        Currently no data is passed to this argument for this method but\n        that may change in the future.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_test_end","title":"<code>on_test_end(logs=None)</code>","text":"<p>Called at the end of evaluation or validation.</p> <p>Subclasses should override for any actions to run.</p> <p>Parameters:</p> Name Type Description Default <code>logs</code> <code>dict</code> <p>Currently the output of the last call to <code>on_test_batch_end()</code> is passed to this argument for this method but that may change in the future.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_test_end(self, logs=None):\n    \"\"\"\n    Called at the end of evaluation or validation.\n\n    Subclasses should override for any actions to run.\n\n    Parameters\n    ----------\n    logs : dict, optional\n        Currently the output of the last call to `on_test_batch_end()` is\n        passed to this argument for this method but that may change in the\n        future.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_predict_begin","title":"<code>on_predict_begin(logs=None)</code>","text":"<p>Called at the beginning of prediction.</p> <p>Subclasses should override for any actions to run.</p> <p>Parameters:</p> Name Type Description Default <code>logs</code> <code>dict</code> <p>Currently no data is passed to this argument for this method but that may change in the future.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_predict_begin(self, logs=None):\n    \"\"\"\n    Called at the beginning of prediction.\n\n    Subclasses should override for any actions to run.\n\n    Parameters\n    ----------\n    logs : dict, optional\n        Currently no data is passed to this argument for this method but\n        that may change in the future.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_predict_end","title":"<code>on_predict_end(logs=None)</code>","text":"<p>Called at the end of prediction.</p> <p>Subclasses should override for any actions to run.</p> <p>Parameters:</p> Name Type Description Default <code>logs</code> <code>dict</code> <p>Currently no data is passed to this argument for this method but that may change in the future.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_predict_end(self, logs=None):\n    \"\"\"\n    Called at the end of prediction.\n\n    Subclasses should override for any actions to run.\n\n    Parameters\n    ----------\n    logs : dict, optional\n        Currently no data is passed to this argument for this method but\n        that may change in the future.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_before_backward","title":"<code>on_before_backward(logs=None)</code>","text":"<p>Called before the backward pass.</p> <p>Subclasses should override for any actions to run.</p> <p>Parameters:</p> Name Type Description Default <code>logs</code> <code>dict</code> <p>Currently no data is passed to this argument for this method but that may change in the future.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_before_backward(self, logs=None):\n    \"\"\"\n    Called before the backward pass.\n\n    Subclasses should override for any actions to run.\n\n    Parameters\n    ----------\n    logs : dict, optional\n        Currently no data is passed to this argument for this method but\n        that may change in the future.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_after_backward","title":"<code>on_after_backward(logs=None)</code>","text":"<p>Called after the backward pass.</p> <p>Subclasses should override for any actions to run.</p> <p>Parameters:</p> Name Type Description Default <code>logs</code> <code>dict</code> <p>Currently no data is passed to this argument for this method but that may change in the future.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_after_backward(self, logs=None):\n    \"\"\"\n    Called after the backward pass.\n\n    Subclasses should override for any actions to run.\n\n    Parameters\n    ----------\n    logs : dict, optional\n        Currently no data is passed to this argument for this method but\n        that may change in the future.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_before_zero_grad","title":"<code>on_before_zero_grad(optimizer=None)</code>","text":"<p>Called before the optimizer's <code>zero_grad</code> method is called.</p> <p>Subclasses should override for any actions to run.</p> <p>Parameters:</p> Name Type Description Default <code>optimizer</code> <code>Optimizer</code> <p>The optimizer that is used for the training step.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_before_zero_grad(self, optimizer: Optimizer | None = None):\n    \"\"\"\n    Called before the optimizer's `zero_grad` method is called.\n\n    Subclasses should override for any actions to run.\n\n    Parameters\n    ----------\n    optimizer : Optimizer, optional\n        The optimizer that is used for the training step.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.Callback.on_before_optimizer_step","title":"<code>on_before_optimizer_step(optimizer=None)</code>","text":"<p>Called before the optimizer step.</p> <p>Subclasses should override for any actions to run.</p> <p>Parameters:</p> Name Type Description Default <code>optimizer</code> <code>Optimizer</code> <p>The optimizer that is used for the training step.</p> <code>None</code> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def on_before_optimizer_step(self, optimizer: Optimizer | None = None):\n    \"\"\"\n    Called before the optimizer step.\n\n    Subclasses should override for any actions to run.\n\n    Parameters\n    ----------\n    optimizer : Optimizer, optional\n        The optimizer that is used for the training step.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.CallbackList","title":"<code>CallbackList(callbacks=None)</code>","text":"<p>               Bases: <code>Callback</code>, <code>Sequence[Callback]</code></p> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def __init__(self, callbacks: list[Callback] | None = None):\n    super().__init__()\n    self._callbacks: list[Callback] = []\n    self.extend(callbacks)\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.base.CallbackList-functions","title":"Functions","text":""},{"location":"api/nn/#tklearn.nn.callbacks.base.CallbackList.copy","title":"<code>copy()</code>","text":"<p>Return a shallow copy of the list.</p> Source code in <code>src/tklearn/nn/callbacks/base.py</code> <pre><code>def copy(self) -&gt; Self:\n    \"\"\"Return a shallow copy of the list.\"\"\"\n    return self.__class__(self._callbacks.copy())\n</code></pre>"},{"location":"api/nn/#checkpoint","title":"Checkpoint","text":""},{"location":"api/nn/#tklearn.nn.callbacks.checkpoint","title":"<code>checkpoint</code>","text":""},{"location":"api/nn/#tklearn.nn.callbacks.checkpoint-classes","title":"Classes","text":""},{"location":"api/nn/#early-stopping","title":"Early Stopping","text":""},{"location":"api/nn/#tklearn.nn.callbacks.early_stopping","title":"<code>early_stopping</code>","text":""},{"location":"api/nn/#tklearn.nn.callbacks.early_stopping-classes","title":"Classes","text":""},{"location":"api/nn/#history","title":"History","text":""},{"location":"api/nn/#tklearn.nn.callbacks.history","title":"<code>history</code>","text":""},{"location":"api/nn/#tklearn.nn.callbacks.history-classes","title":"Classes","text":""},{"location":"api/nn/#plateau-early-stopping","title":"Plateau Early Stopping","text":""},{"location":"api/nn/#tklearn.nn.callbacks.plateau_early_stopping","title":"<code>plateau_early_stopping</code>","text":""},{"location":"api/nn/#tklearn.nn.callbacks.plateau_early_stopping-classes","title":"Classes","text":""},{"location":"api/nn/#progress-bar-logger","title":"Progress Bar Logger","text":""},{"location":"api/nn/#tklearn.nn.callbacks.progbar_logger","title":"<code>progbar_logger</code>","text":""},{"location":"api/nn/#tklearn.nn.callbacks.progbar_logger-classes","title":"Classes","text":""},{"location":"api/nn/#tklearn.nn.callbacks.progbar_logger.ProgbarLogger","title":"<code>ProgbarLogger(exclude=None, epoch_desc='Epoch', pred_desc='Predicting', batch_desc='Batch')</code>","text":"<p>               Bases: <code>Callback</code></p> <p>Callback to display progress bars and performance reports during training and prediction. Uses tqdm for progress visualization.</p> Source code in <code>src/tklearn/nn/callbacks/progbar_logger.py</code> <pre><code>def __init__(\n    self,\n    exclude: Optional[list] = None,\n    epoch_desc: str = \"Epoch\",\n    pred_desc: str = \"Predicting\",\n    batch_desc: str = \"Batch\",\n):\n    super().__init__()\n    self.pred_desc = pred_desc\n    self.epoch_desc = epoch_desc\n    self.batch_desc = batch_desc\n\n    self.exclude = exclude\n\n    self.training = False\n\n    self.train_epochs = None\n    self.train_steps = None  # Corrected typo from original code\n    self.pred_steps = None\n\n    self._zero_based_train_step = False\n    self._zero_based_train_epoch = False\n    self._zero_based_pred_step = False\n\n    # tqdm progress bar instances\n    self.epoch_progress_bar: Optional[tqdm.tqdm] = None\n    self.batch_progress_bar: Optional[tqdm.tqdm] = None\n    self.predict_progress_bar: Optional[tqdm.tqdm] = None\n\n    # history of performance reports\n    self.history = []\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.progbar_logger.ProgbarLogger-functions","title":"Functions","text":""},{"location":"api/nn/#tklearn.nn.callbacks.progbar_logger.ProgbarLogger.on_train_begin","title":"<code>on_train_begin(logs=None)</code>","text":"<p>Initializes the progress bar for training epochs.</p> Source code in <code>src/tklearn/nn/callbacks/progbar_logger.py</code> <pre><code>def on_train_begin(self, logs=None):\n    \"\"\"\n    Initializes the progress bar for training epochs.\n    \"\"\"\n    self.training = True\n    self.train_epochs = self.params.get(\"epochs\")  # Use .get() for safety\n    self.train_steps = self.params.get(\"steps\")  # Use .get() for safety\n\n    if self.train_epochs is not None:\n        # initialize the progress bar for training epochs\n        self.epoch_progress_bar = tqdm.tqdm(\n            total=self.train_epochs,\n            desc=self.epoch_desc,\n            unit=\"epoch\",\n            leave=True,  # Keep the bar after completion\n        )\n\n    self.history = []  # Reset history at the start of training\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.progbar_logger.ProgbarLogger.on_train_batch_begin","title":"<code>on_train_batch_begin(batch, logs=None)</code>","text":"<p>Notes if batch indexing is zero-based. Initializes batch progress bar at the start of each epoch's first batch.</p> Source code in <code>src/tklearn/nn/callbacks/progbar_logger.py</code> <pre><code>def on_train_batch_begin(self, batch, logs=None):\n    \"\"\"\n    Notes if batch indexing is zero-based.\n    Initializes batch progress bar at the start of each epoch's first batch.\n    \"\"\"\n    if batch == 0:\n        self._zero_based_train_step = True\n        # Initialize batch progress bar at the start of a new epoch\n        if self.train_steps is not None:\n            self.batch_progress_bar = tqdm.tqdm(\n                total=self.train_steps,\n                desc=self.batch_desc,\n                unit=\"batch\",\n                leave=False,  # Remove the bar after completion of the epoch\n            )\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.progbar_logger.ProgbarLogger.on_train_batch_end","title":"<code>on_train_batch_end(batch, logs=None)</code>","text":"<p>Updates the training step progress bar with current batch logs.</p> Source code in <code>src/tklearn/nn/callbacks/progbar_logger.py</code> <pre><code>def on_train_batch_end(self, batch, logs=None):\n    \"\"\"\n    Updates the training step progress bar with current batch logs.\n    \"\"\"\n    # if self._zero_based_train_step:\n    #     natural_step = batch + 1\n    # else:\n    #     natural_step = batch\n    # update the training step progress bar\n    if self.batch_progress_bar is not None:\n        # Increment by 1 for the completed batch\n        self.batch_progress_bar.update(1)\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.progbar_logger.ProgbarLogger.on_epoch_begin","title":"<code>on_epoch_begin(epoch, logs=None)</code>","text":"<p>Notes if epoch indexing is zero-based.</p> Source code in <code>src/tklearn/nn/callbacks/progbar_logger.py</code> <pre><code>def on_epoch_begin(self, epoch, logs=None):\n    \"\"\"\n    Notes if epoch indexing is zero-based.\n    \"\"\"\n    # Assuming 'epoch' here is the zero-based index provided by the trainer\n    self.current_epoch = epoch  # Store current epoch index\n    if epoch == 0:\n        self._zero_based_train_epoch = True\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.progbar_logger.ProgbarLogger.on_epoch_end","title":"<code>on_epoch_end(epoch, logs=None)</code>","text":"<p>Finalizes the progress bar for the current epoch.</p> Source code in <code>src/tklearn/nn/callbacks/progbar_logger.py</code> <pre><code>def on_epoch_end(self, epoch, logs=None):\n    \"\"\"Finalizes the progress bar for the current epoch.\"\"\"\n    # Increment by 1 for the completed epoch\n    if self.epoch_progress_bar is not None:\n        self.epoch_progress_bar.update(1)\n\n    # Close the batch progress bar for this epoch\n    if self.batch_progress_bar is not None:\n        self.batch_progress_bar.close()\n        self.batch_progress_bar = None  # Reset for the next epoch\n\n    # update table for the current epoch\n    if self._zero_based_train_epoch:\n        natural_epoch = epoch + 1\n    else:\n        natural_epoch = epoch\n    report = get_performance_report(\n        logs,\n        exclude=self.exclude,\n        epoch=natural_epoch,\n    )\n\n    self.history.append(report)\n\n    # Use print directly as tqdm manages console output\n    df = pd.DataFrame(self.history)\n    tqdm.tqdm.write(\n        \"\\n{}\\n\".format(tabulate(df, headers=\"keys\", tablefmt=\"pipe\"))\n    )\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.progbar_logger.ProgbarLogger.on_predict_begin","title":"<code>on_predict_begin(logs=None)</code>","text":"<p>Initializes the progress bar for prediction steps.</p> Source code in <code>src/tklearn/nn/callbacks/progbar_logger.py</code> <pre><code>def on_predict_begin(self, logs=None):\n    \"\"\"\n    Initializes the progress bar for prediction steps.\n    \"\"\"\n    if \"pred_steps\" not in self.params:\n        return\n    total = self.params[\"pred_steps\"]\n    # initialize the progress bar for prediction steps\n    self.predict_progress_bar = tqdm.tqdm(\n        total=total,\n        desc=self.pred_desc,\n        unit=\"step\",\n        leave=True,  # Keep the bar after completion\n    )\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.progbar_logger.ProgbarLogger.on_predict_batch_begin","title":"<code>on_predict_batch_begin(batch, logs=None)</code>","text":"<p>Notes if prediction batch indexing is zero-based.</p> Source code in <code>src/tklearn/nn/callbacks/progbar_logger.py</code> <pre><code>def on_predict_batch_begin(self, batch, logs=None):\n    \"\"\"\n    Notes if prediction batch indexing is zero-based.\n    \"\"\"\n    if batch == 0:\n        self._zero_based_pred_step = True\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.progbar_logger.ProgbarLogger.on_predict_batch_end","title":"<code>on_predict_batch_end(batch, logs=None)</code>","text":"<p>Updates the prediction step progress bar.</p> Source code in <code>src/tklearn/nn/callbacks/progbar_logger.py</code> <pre><code>def on_predict_batch_end(self, batch, logs=None):\n    \"\"\"\n    Updates the prediction step progress bar.\n    \"\"\"\n    # update the prediction step progress bar\n    if self.predict_progress_bar is None:\n        return\n    # Increment by 1 for the completed batch\n    self.predict_progress_bar.update(1)\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.progbar_logger.ProgbarLogger.on_predict_end","title":"<code>on_predict_end(logs=None)</code>","text":"<p>Finalizes the prediction progress bar.</p> Source code in <code>src/tklearn/nn/callbacks/progbar_logger.py</code> <pre><code>def on_predict_end(self, logs=None):\n    \"\"\"\n    Finalizes the prediction progress bar.\n    \"\"\"\n    if self.predict_progress_bar is None:\n        return\n    # finalize anything stated during prediction\n    self.predict_progress_bar.close()\n    self.predict_progress_bar = None\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.progbar_logger.ProgbarLogger.on_train_end","title":"<code>on_train_end(logs=None)</code>","text":"<p>Finalizes the training epoch progress bar.</p> Source code in <code>src/tklearn/nn/callbacks/progbar_logger.py</code> <pre><code>def on_train_end(self, logs=None):\n    \"\"\"\n    Finalizes the training epoch progress bar.\n    \"\"\"\n    if not self.training:\n        return\n    # finalize anything stated during training\n    if self.epoch_progress_bar is None:\n        return\n    self.epoch_progress_bar.close()\n    self.epoch_progress_bar = None\n</code></pre>"},{"location":"api/nn/#tklearn.nn.callbacks.progbar_logger-functions","title":"Functions","text":""},{"location":"api/nn/#tklearn.nn.callbacks.progbar_logger.get_performance_report","title":"<code>get_performance_report(logs, exclude=None, epoch=None)</code>","text":"<p>Formats logs into a performance report dictionary.</p> Source code in <code>src/tklearn/nn/callbacks/progbar_logger.py</code> <pre><code>def get_performance_report(logs, exclude=None, epoch=None) -&gt; dict[str, str]:\n    \"\"\"\n    Formats logs into a performance report dictionary.\n    \"\"\"\n    if logs is None:\n        logs = {}\n    row = {}\n    for k, v in logs.items():\n        if exclude is not None and k in exclude:\n            continue\n        # Format float values to 4 decimal places\n        if isinstance(v, float):\n            v = f\"{v:0.4f}\"\n        # Format integer values\n        elif isinstance(v, int):\n            v = f\"{v:d}\"\n        # Truncate string values if too long\n        elif isinstance(v, str):\n            v = (v[:10] + \"...\") if len(v) &gt; 10 else v\n        else:\n            # Skip other types\n            continue\n        row[k] = v\n    # Add epoch number if provided\n    if epoch is not None:\n        row[\"epoch\"] = f\"{epoch}\"\n    return row\n</code></pre>"},{"location":"api/nn/#tracking","title":"Tracking","text":""},{"location":"api/nn/#tklearn.nn.callbacks.tracking","title":"<code>tracking</code>","text":""},{"location":"api/nn/#tklearn.nn.callbacks.tracking-classes","title":"Classes","text":""},{"location":"api/nn/#models","title":"Models","text":""},{"location":"api/nn/#base-model","title":"Base Model","text":""},{"location":"api/nn/#tklearn.nn.models.base","title":"<code>base</code>","text":""},{"location":"api/nn/#tklearn.nn.models.base-classes","title":"Classes","text":""},{"location":"api/nn/#backbone-models","title":"Backbone Models","text":""},{"location":"api/nn/#adapter","title":"Adapter","text":""},{"location":"api/nn/#tklearn.nn.models.backbone.adapter","title":"<code>adapter</code>","text":""},{"location":"api/nn/#tklearn.nn.models.backbone.adapter-classes","title":"Classes","text":""},{"location":"api/nn/#tklearn.nn.models.backbone.adapter.Adapter","title":"<code>Adapter</code>","text":"<p>               Bases: <code>Backbone</code></p>"},{"location":"api/nn/#tklearn.nn.models.backbone.adapter.Adapter-attributes","title":"Attributes","text":""},{"location":"api/nn/#tklearn.nn.models.backbone.adapter.Adapter.features","title":"<code>features</code>  <code>property</code>","text":"<p>Get the signature of the model using the tokenizer.</p>"},{"location":"api/nn/#base-backbone","title":"Base Backbone","text":""},{"location":"api/nn/#tklearn.nn.models.backbone.base","title":"<code>base</code>","text":""},{"location":"api/nn/#transformer","title":"Transformer","text":""},{"location":"api/nn/#tklearn.nn.models.backbone.transformer","title":"<code>transformer</code>","text":""},{"location":"api/nn/#tklearn.nn.models.backbone.transformer-classes","title":"Classes","text":""},{"location":"api/nn/#tklearn.nn.models.backbone.transformer.Transformer","title":"<code>Transformer</code>","text":"<p>               Bases: <code>Backbone</code></p>"},{"location":"api/nn/#tklearn.nn.models.backbone.transformer.Transformer-attributes","title":"Attributes","text":""},{"location":"api/nn/#tklearn.nn.models.backbone.transformer.Transformer.features","title":"<code>features</code>  <code>property</code>","text":"<p>Get the signature of the model using the tokenizer.</p>"},{"location":"api/nn/#kbert-models","title":"KBERT Models","text":""},{"location":"api/nn/#tklearn.nn.models.kbert.helpers","title":"<code>helpers</code>","text":""},{"location":"api/nn/#tklearn.nn.models.kbert.helpers-classes","title":"Classes","text":""},{"location":"api/nn/#tklearn.nn.models.kbert.helpers.Injection","title":"<code>Injection(text, target_index, _resolved_index=0, _final_start=0, _final_end=0)</code>  <code>dataclass</code>","text":""},{"location":"api/nn/#tklearn.nn.models.kbert.helpers.Injection-attributes","title":"Attributes","text":""},{"location":"api/nn/#tklearn.nn.models.kbert.helpers.Injection.truncated_span","title":"<code>truncated_span</code>  <code>property</code>","text":"<p>Calculates the span of the injected text, excluding leading/trailing whitespace. Collapses to a zero-length span if the text is entirely whitespace.</p>"},{"location":"api/nn/#tklearn.nn.models.kbert.helpers-functions","title":"Functions","text":""},{"location":"api/nn/#tklearn.nn.models.kbert.helpers.inject","title":"<code>inject(text, injections, spans)</code>","text":"<p>Injects text snippets into the original text at specified indices, adjusting for collisions with existing spans.</p> <p>Complexity: O(N log N + M log M) where N=spans, M=injections.</p> Source code in <code>src/tklearn/nn/models/kbert/helpers.py</code> <pre><code>def inject(\n    text: str, injections: list[Injection], spans: list[Span]\n) -&gt; tuple[str, list[Span], list[Span]]:\n    \"\"\"Injects text snippets into the original text at specified indices,\n    adjusting for collisions with existing spans.\n\n    Complexity: O(N log N + M log M) where N=spans, M=injections.\n    \"\"\"\n\n    if not injections:\n        return text, spans, []\n\n    # --- Step 1: Efficient Collision Resolution ---\n    # Instead of cascading iteratively, we merge overlaps and check once.\n\n    merged_spans = _merge_spans(spans)\n    merged_starts = [s.start for s in merged_spans]\n\n    for inj in injections:\n        curr = inj.target_index\n\n        # Find the span that starts before or at the target index.\n        # bisect_right returns insertion point to keep list sorted.\n        idx = bisect.bisect_right(merged_starts, curr) - 1\n\n        if idx &gt;= 0:\n            candidate = merged_spans[idx]\n            # Strict inclusion check: start &lt; curr &lt; end\n            if candidate.start &lt; curr &lt; candidate.end:\n                curr = candidate.end\n\n        inj._resolved_index = curr\n\n    # --- Step 2: Sort Injections ---\n    # Stable sort ensures deterministic order for injections at the same index\n    injections.sort(key=lambda x: x._resolved_index)\n\n    # --- Step 3: Build Text &amp; Calculate Offsets ---\n\n    result_parts = []\n    current_text_idx = 0\n    cumulative_shift = 0\n\n    # Store shifts for Step 4: (at_original_index, amount_added)\n    injection_shifts = []\n\n    for inj in injections:\n        # 1. Append text from the last injection point up to this one\n        if current_text_idx &lt; inj._resolved_index:\n            result_parts.append(text[current_text_idx : inj._resolved_index])\n            current_text_idx = inj._resolved_index\n\n        # 2. Append the injection\n        result_parts.append(inj.text)\n\n        inj_len = len(inj.text)\n\n        # 3. Calculate final positions for this injection\n        final_start = inj._resolved_index + cumulative_shift\n        inj._final_start = final_start\n        inj._final_end = final_start + inj_len\n\n        # 4. Track shift\n        cumulative_shift += inj_len\n        injection_shifts.append((inj._resolved_index, inj_len))\n\n    # Append remaining original text\n    result_parts.append(text[current_text_idx:])\n    final_text = \"\".join(result_parts)\n\n    # --- Step 4: Update Original Spans (Linear Sweep) ---\n\n    new_spans = []\n    # We must iterate original spans in order to match shifts correctly\n    sorted_spans = sorted(spans, key=lambda s: s.start)\n\n    current_shift = 0\n    inj_idx = 0\n    n_injections = len(injection_shifts)\n\n    for span in sorted_spans:\n        # Advance the injection pointer to apply all shifts that occur\n        # at or before the start of the current span.\n        while inj_idx &lt; n_injections:\n            r_index, length = injection_shifts[inj_idx]\n            if r_index &lt;= span.start:\n                current_shift += length\n                inj_idx += 1\n            else:\n                break\n\n        new_spans.append(\n            Span(span.start + current_shift, span.end + current_shift)\n        )\n\n    # --- Step 5: Collect Injection Spans ---\n    injection_spans = [i.truncated_span for i in injections]\n\n    return final_text, new_spans, injection_spans\n</code></pre>"},{"location":"api/nn/#tklearn.nn.models.kbert.model","title":"<code>model</code>","text":""},{"location":"api/nn/#tklearn.nn.models.kbert.model-classes","title":"Classes","text":""},{"location":"api/nn/#tklearn.nn.models.kbert.model.KBertModel","title":"<code>KBertModel(base_model)</code>","text":"<p>               Bases: <code>Module</code></p> Source code in <code>src/tklearn/nn/models/kbert/model.py</code> <pre><code>def __init__(self, base_model: PreTrainedModel):\n    super().__init__()\n    # Locate the correct encoder to patch.\n    # HF models expose the inner encoder via the `.base_model` property.\n    # - If 'base_model' is a Classifier, .base_model returns the inner BERT/RoBERTa.\n    # - If 'base_model' is already the bare BERT, .base_model returns self.\n    target_encoder = base_model.base_model\n    if not hasattr(target_encoder, \"get_extended_attention_mask\"):\n        raise ValueError(UNSUPORTED_MODEL_ERROR)\n    # 2. Patch the method on the specific instance\n    target_encoder.get_extended_attention_mask = types.MethodType(\n        get_extended_attention_mask, target_encoder\n    )\n    self.base_model = base_model\n</code></pre>"},{"location":"api/nn/#tklearn.nn.models.kbert.model.KBertModel-functions","title":"Functions","text":""},{"location":"api/nn/#tklearn.nn.models.kbert.model.KBertModel.from_pretrained","title":"<code>from_pretrained(pretrained_model_name_or_path, model_cls=None, **kwargs)</code>  <code>classmethod</code>","text":"<p>Load a pretrained model and wrap it.</p> <p>Args:     pretrained_model_name_or_path: Path or HF Hub ID.     model_cls: The specific HF class to load (e.g., AutoModel,                AutoModelForSequenceClassification). Defaults to AutoModel.     **kwargs: Passed to the HF from_pretrained method.</p> Source code in <code>src/tklearn/nn/models/kbert/model.py</code> <pre><code>@classmethod\ndef from_pretrained(\n    cls,\n    pretrained_model_name_or_path: Union[str, Any],\n    model_cls: Type[PreTrainedModel] | None = None,\n    **kwargs,\n):\n    \"\"\"\n    Load a pretrained model and wrap it.\n\n    Args:\n        pretrained_model_name_or_path: Path or HF Hub ID.\n        model_cls: The specific HF class to load (e.g., AutoModel,\n                   AutoModelForSequenceClassification). Defaults to AutoModel.\n        **kwargs: Passed to the HF from_pretrained method.\n    \"\"\"\n    if model_cls is None:\n        model_cls = AutoModel\n    if \"attn_implementation\" not in kwargs:\n        kwargs[\"attn_implementation\"] = \"eager\"\n    # Instantiate the requested model architecture\n    base_model = model_cls.from_pretrained(\n        pretrained_model_name_or_path, **kwargs\n    )\n    return cls(base_model)\n</code></pre>"},{"location":"api/nn/#tklearn.nn.models.kbert.model-functions","title":"Functions","text":""},{"location":"api/nn/#tklearn.nn.models.kbert.model.get_extended_attention_mask","title":"<code>get_extended_attention_mask(self, attention_mask, input_shape, device=None, dtype=None)</code>","text":"<p>Makes broadcastable attention and causal masks so that future and masked tokens are ignored.</p> <p>Arguments:     attention_mask (<code>torch.Tensor</code>):         Mask with ones indicating tokens to attend to, zeros for tokens to ignore.     input_shape (<code>Tuple[int]</code>):         The shape of the input to the model.</p> <p>Returns:     <code>torch.Tensor</code> The extended attention mask, with a the same dtype as <code>attention_mask.dtype</code>.</p> Source code in <code>src/tklearn/nn/models/kbert/model.py</code> <pre><code>def get_extended_attention_mask(\n    self: PreTrainedModel,\n    attention_mask: torch.Tensor,\n    input_shape: tuple[int],\n    device: torch.device = None,\n    dtype: torch.dtype = None,\n) -&gt; torch.Tensor:\n    \"\"\"\n    Makes broadcastable attention and causal masks so that future and masked tokens are ignored.\n\n    Arguments:\n        attention_mask (`torch.Tensor`):\n            Mask with ones indicating tokens to attend to, zeros for tokens to ignore.\n        input_shape (`Tuple[int]`):\n            The shape of the input to the model.\n\n    Returns:\n        `torch.Tensor` The extended attention mask, with a the same dtype as `attention_mask.dtype`.\n    \"\"\"\n    if dtype is None:\n        dtype = self.dtype\n\n    if not (attention_mask.dim() == 2 and self.config.is_decoder):\n        # show warning only if it won't be shown in `create_extended_attention_mask_for_decoder`\n        if device is not None:\n            warnings.warn(\n                \"The `device` argument is deprecated and will be removed in v5 of Transformers.\",\n                FutureWarning,\n            )\n    # We can provide a self-attention mask of dimensions [batch_size, from_seq_length, to_seq_length]\n    # ourselves in which case we just need to make it broadcastable to all heads.\n    if attention_mask.dim() == 4:\n        extended_attention_mask = attention_mask\n    if attention_mask.dim() == 3:\n        extended_attention_mask = attention_mask[:, None, :, :]\n    elif attention_mask.dim() == 2:\n        # Provided a padding mask of dimensions [batch_size, seq_length]\n        # - if the model is a decoder, apply a causal mask in addition to the padding mask\n        # - if the model is an encoder, make the mask broadcastable to [batch_size, num_heads, seq_length, seq_length]\n        if self.config.is_decoder:\n            extended_attention_mask = (\n                ModuleUtilsMixin.create_extended_attention_mask_for_decoder(\n                    input_shape, attention_mask, device\n                )\n            )\n        else:\n            extended_attention_mask = attention_mask[:, None, None, :]\n    else:\n        raise ValueError(\n            f\"Wrong shape for input_ids (shape {input_shape}) or attention_mask (shape {attention_mask.shape})\"\n        )\n\n    # Since attention_mask is 1.0 for positions we want to attend and 0.0 for\n    # masked positions, this operation will create a tensor which is 0.0 for\n    # positions we want to attend and the dtype's smallest value for masked positions.\n    # Since we are adding it to the raw scores before the softmax, this is\n    # effectively the same as removing these entirely.\n    extended_attention_mask = extended_attention_mask.to(dtype=dtype)\n\n    # fp16 compatibility\n    extended_attention_mask = (1.0 - extended_attention_mask) * torch.finfo(\n        dtype\n    ).min\n\n    return extended_attention_mask\n</code></pre>"},{"location":"api/nn/#tklearn.nn.models.kbert.tokenizer","title":"<code>tokenizer</code>","text":""},{"location":"api/nn/#tklearn.nn.models.kbert.tokenizer-classes","title":"Classes","text":""},{"location":"api/nn/#tklearn.nn.models.kbert.tokenizer.KBertTokenizer","title":"<code>KBertTokenizer(config)</code>","text":"Source code in <code>src/tklearn/nn/models/kbert/tokenizer.py</code> <pre><code>def __init__(\n    self,\n    config: KBertTokenizerLegacyConfig | KBertTokenizerLegacyConfigDict,\n):\n    if isinstance(config, dict):\n        config = KBertTokenizerLegacyConfig.from_dict(config)\n    self.config = config\n    self.__post_init__()\n</code></pre>"},{"location":"api/nn/#tklearn.nn.models.kbert.tokenizer.KBertTokenizer-functions","title":"Functions","text":""},{"location":"api/nn/#tklearn.nn.models.kbert.tokenizer.KBertTokenizer.fit","title":"<code>fit(raw_documents, y=None)</code>","text":"<p>Fit the tokenizer using feature scoring methods.</p> <p>This method extracts triples from the documents and scores them using three different feature selection methods:</p> <ol> <li> <p>Chi-Square (chi2_score): Measures the dependence between each    predicate-object pair and the class labels. Higher scores indicate    that the feature's occurrence is more dependent on the class,    making it useful for classification. Chi-square is best suited for    non-negative feature values (e.g., term counts).</p> </li> <li> <p>Mutual Information (mutual_info): Quantifies the amount of    information that a predicate-object pair provides about the class    label. Unlike chi-square, mutual information can capture non-linear    dependencies between features and labels. A score of zero indicates    independence between the feature and the class.</p> </li> <li> <p>SVM Coefficients (svm_score): Uses the absolute values of    coefficients from a trained Linear Support Vector Classifier    (LinearSVC). Features with larger absolute coefficients have a    stronger influence on the decision boundary. For binary    classification, the absolute coefficient is used; for multiclass,    the maximum absolute coefficient across all classes is taken.</p> </li> </ol> <p>Parameters:</p> Name Type Description Default <code>raw_documents</code> <code>list[str]</code> <p>List of raw text documents.</p> required <code>y</code> <code>list | None</code> <p>List of labels for each document.</p> <code>None</code> Source code in <code>src/tklearn/nn/models/kbert/tokenizer.py</code> <pre><code>def fit(self, raw_documents: list[str], y: list | None = None):\n    \"\"\"\n    Fit the tokenizer using feature scoring methods.\n\n    This method extracts triples from the documents and scores them using\n    three different feature selection methods:\n\n    1. **Chi-Square (chi2_score)**: Measures the dependence between each\n       predicate-object pair and the class labels. Higher scores indicate\n       that the feature's occurrence is more dependent on the class,\n       making it useful for classification. Chi-square is best suited for\n       non-negative feature values (e.g., term counts).\n\n    2. **Mutual Information (mutual_info)**: Quantifies the amount of\n       information that a predicate-object pair provides about the class\n       label. Unlike chi-square, mutual information can capture non-linear\n       dependencies between features and labels. A score of zero indicates\n       independence between the feature and the class.\n\n    3. **SVM Coefficients (svm_score)**: Uses the absolute values of\n       coefficients from a trained Linear Support Vector Classifier\n       (LinearSVC). Features with larger absolute coefficients have a\n       stronger influence on the decision boundary. For binary\n       classification, the absolute coefficient is used; for multiclass,\n       the maximum absolute coefficient across all classes is taken.\n\n    Parameters\n    ----------\n    raw_documents : list[str]\n        List of raw text documents.\n    y : list | None\n        List of labels for each document.\n    \"\"\"\n    triples = []\n    desc = \"Extracting Triples\"\n    pbar = tqdm.tqdm(\n        enumerate(zip(raw_documents, y)),\n        total=len(raw_documents),\n        desc=desc,\n    )\n    for idx, (text, label) in pbar:\n        for triple in self.extract_triples(text=text, return_all=True)[0]:\n            subject, predicate, object_ = triple\n            triples.append({\n                \"text\": text,\n                \"label\": label,\n                \"subject\": subject,\n                \"predicate\": predicate,\n                \"object\": object_,\n                \"idx\": idx,\n                \"predicate_object\": f\"{predicate}:{object_}\",\n            })\n    df = pd.DataFrame(triples)\n    # Calculate diversity (number of unique subjects per predicate-object)\n    diversity = (\n        df\n        .groupby(\"predicate_object\")[\"subject\"]\n        .nunique()\n        .rename(\"diversity\")\n    )\n    # Prepare data for feature scoring\n    temp_df = (\n        df\n        .groupby(\"idx\")\n        .agg({\"predicate_object\": lambda x: list(x), \"label\": \"first\"})\n        .reset_index()\n    )\n    labels = temp_df[\"label\"].values\n    # Use TF-IDF vectorizer\n    # tfidf = TfidfVectorizer(\n    #     tokenizer=passthrough,\n    #     preprocessor=passthrough,\n    #     lowercase=False,\n    #     token_pattern=None,\n    # )\n    cv = CountVectorizer(\n        tokenizer=passthrough,\n        preprocessor=passthrough,\n        lowercase=False,\n        token_pattern=None,\n    )\n    X = cv.fit_transform(temp_df[\"predicate_object\"])\n    # Feature names\n    feature_names = cv.get_feature_names_out()\n    # # Multiply counts by diversity to get weighted counts\n    # diversity_weights = np.array([\n    #     diversity.get(fn, 1.0) for fn in feature_names\n    # ])\n    # # Apply diversity weights\n    # X = X.multiply(diversity_weights)\n    # Calculate Chi-Square\n    chi2_scores, p_values = chi2(X, labels)\n    # Calculate Mutual Information (Information Gain)\n    mi_scores = mutual_info_classif(X, labels, discrete_features=True)\n    # Train LinearSVC to get feature importance via coefficients\n    clf = LinearSVC(max_iter=10000, dual=\"auto\")\n    clf.fit(X, labels)\n    # Extract feature scores from SVM coefficients\n    # For binary classification: coef_ is shape (1, n_features)\n    # For multiclass: coef_ is shape (n_classes, n_features)\n    if clf.coef_.shape[0] == 1:\n        # Binary classification: use absolute coefficients\n        svm_scores = np.abs(clf.coef_[0])\n    else:\n        # Multiclass: use max absolute coefficient across classes\n        svm_scores = np.abs(clf.coef_).max(axis=0)\n    # Store the scores in a DataFrame\n    doc_counts = (X &gt; 0).sum(axis=0).tolist()[0]\n    triple_scores = pd.DataFrame(\n        {\n            \"svm_score\": svm_scores,\n            \"doc_counts\": doc_counts,\n            \"chi2_score\": chi2_scores,\n            \"mutual_info\": mi_scores,\n        },\n        index=feature_names,\n    )\n    self.triple_scores = triple_scores.join(diversity)\n    self.threshold = None\n    if len(self.triple_scores) &lt; self.config.threshold_top_k:\n        return\n    self.threshold = (\n        self\n        .get_score(self.triple_scores)\n        .sort_values(ascending=False)\n        .iloc[self.config.threshold_top_k - 1]\n    ).item()\n</code></pre>"},{"location":"api/nn/#tklearn.nn.models.kbert.tokenizer-functions","title":"Functions","text":""},{"location":"api/nn/#tklearn.nn.models.kbert.collator","title":"<code>collator</code>","text":""},{"location":"api/nn/#tklearn.nn.models.kbert.collator-classes","title":"Classes","text":""},{"location":"api/nn/#classifiers","title":"Classifiers","text":""},{"location":"api/nn/#helpers","title":"Helpers","text":""},{"location":"api/nn/#tklearn.nn.models.classifier.helpers","title":"<code>helpers</code>","text":""},{"location":"api/nn/#tklearn.nn.models.classifier.helpers-classes","title":"Classes","text":""},{"location":"api/nn/#tklearn.nn.models.classifier.helpers-functions","title":"Functions","text":""},{"location":"api/nn/#multiclass-classifier","title":"Multiclass Classifier","text":""},{"location":"api/nn/#tklearn.nn.models.classifier.multiclass","title":"<code>multiclass</code>","text":""},{"location":"api/nn/#utilities","title":"Utilities","text":""},{"location":"api/nn/#collators","title":"Collators","text":""},{"location":"api/nn/#tklearn.nn.utils.collators","title":"<code>collators</code>","text":""},{"location":"api/nn/#tklearn.nn.utils.collators-attributes","title":"Attributes","text":""},{"location":"api/nn/#tklearn.nn.utils.collators.InputDataClass","title":"<code>InputDataClass = NewType('InputDataClass', Any)</code>  <code>module-attribute</code>","text":"<p>A DataCollator is a function that takes a list of samples from a Dataset and collate them into a batch, as a dictionar of PyTorch/TensorFlow tensors or NumPy arrays.</p>"},{"location":"api/nn/#tklearn.nn.utils.collators-classes","title":"Classes","text":""},{"location":"api/nn/#tklearn.nn.utils.collators.DefaultDataCollator","title":"<code>DefaultDataCollator(return_tensors='pt')</code>  <code>dataclass</code>","text":"<p>               Bases: <code>DataCollatorMixin</code></p> <p>Very simple data collator that simply collates batches of dict-like objects and performs special handling for potential keys named:</p> <pre><code>- `label`: handles a single value (int or float) per object\n- `label_ids`: handles a list of values per object\n</code></pre> <p>Does not do any additional preprocessing: property names of the input object will be used as corresponding inputs to the model. See glue and ner for example of how it's useful.</p> <p>This is an object (like other data collators) rather than a pure function like default_data_collator. This can be helpful if you need to set a return_tensors value at initialization.</p> <p>Args:     return_tensors (<code>str</code>, optional, defaults to <code>\"pt\"</code>):         The type of Tensor to return. Allowable values are \"np\", \"pt\" and \"tf\".</p>"},{"location":"api/nn/#tklearn.nn.utils.collators-functions","title":"Functions","text":""},{"location":"api/nn/#tklearn.nn.utils.collators.default_data_collator","title":"<code>default_data_collator(features, return_tensors='pt')</code>","text":"<p>Very simple data collator that simply collates batches of dict-like objects and performs special handling for potential keys named:</p> <pre><code>- `label`: handles a single value (int or float) per object\n- `label_ids`: handles a list of values per object\n</code></pre> <p>Does not do any additional preprocessing: property names of the input object will be used as corresponding inputs to the model. See glue and ner for example of how it's useful.</p> Source code in <code>src/tklearn/nn/utils/collators.py</code> <pre><code>def default_data_collator(\n    features: list[InputDataClass], return_tensors=\"pt\"\n) -&gt; dict[str, Any]:\n    \"\"\"\n    Very simple data collator that simply collates batches of dict-like objects\n    and performs special handling for potential keys named:\n\n        - `label`: handles a single value (int or float) per object\n        - `label_ids`: handles a list of values per object\n\n    Does not do any additional preprocessing: property names of the input object\n    will be used as corresponding inputs\n    to the model. See glue and ner for example of how it's useful.\n    \"\"\"\n\n    # In this function we'll make the assumption that all `features` in the batch\n    # have the same attributes.\n    # So we will look at the first element as a proxy for what attributes exist\n    # on the whole batch.\n\n    if return_tensors == \"pt\":\n        return torch_default_data_collator(features)\n    raise ValueError(\n        f\"return_tensors should be ''pt', but got {return_tensors}\"\n    )\n</code></pre>"},{"location":"api/nn/#tklearn.nn.utils.collators.pad_without_fast_tokenizer_warning","title":"<code>pad_without_fast_tokenizer_warning(tokenizer, *pad_args, **pad_kwargs)</code>","text":"<p>Pads without triggering the warning about how using the pad function is sub-optimal when using a fast tokenizer.</p> Source code in <code>src/tklearn/nn/utils/collators.py</code> <pre><code>def pad_without_fast_tokenizer_warning(tokenizer, *pad_args, **pad_kwargs):\n    \"\"\"\n    Pads without triggering the warning about how using the pad function\n    is sub-optimal when using a fast tokenizer.\n    \"\"\"\n\n    # To avoid errors when using Feature extractors\n    if not hasattr(tokenizer, \"deprecation_warnings\"):\n        return tokenizer.pad(*pad_args, **pad_kwargs)\n\n    # Save the state of the warning, then disable it\n    warning_state = tokenizer.deprecation_warnings.get(\n        \"Asking-to-pad-a-fast-tokenizer\", False\n    )\n    tokenizer.deprecation_warnings[\"Asking-to-pad-a-fast-tokenizer\"] = True\n\n    try:\n        padded = tokenizer.pad(*pad_args, **pad_kwargs)\n    finally:\n        # Restore the state of the warning.\n        tokenizer.deprecation_warnings[\"Asking-to-pad-a-fast-tokenizer\"] = (\n            warning_state\n        )\n\n    return padded\n</code></pre>"},{"location":"api/nn/#collections","title":"Collections","text":""},{"location":"api/nn/#tklearn.nn.utils.collections","title":"<code>collections</code>","text":""},{"location":"api/nn/#tklearn.nn.utils.collections-classes","title":"Classes","text":""},{"location":"api/nn/#tklearn.nn.utils.collections.TensorDict","title":"<code>TensorDict(*args, **kwargs)</code>","text":"<p>               Bases: <code>Mapping[str, T]</code>, <code>Generic[T]</code></p> Source code in <code>src/tklearn/nn/utils/collections.py</code> <pre><code>def __init__(self, *args, **kwargs):\n    super().__init__()\n    if len(args) &gt; 1:\n        raise ValueError(\n            f\"dict expected at most 1 argument, got {len(args)}\"\n        )\n    if len(args) &gt; 0:\n        if not isinstance(args[0], Mapping):\n            args = {\"arg\": args[0]}\n        else:\n            args = args[0]\n        kwargs.update(args)\n    data: Mapping[str, T] = {}\n    for key, value in dict(**kwargs).items():\n        if value is None:\n            continue\n        data[key] = value\n    self.data = data\n</code></pre>"},{"location":"api/nn/#tklearn.nn.utils.collections.TensorDict-functions","title":"Functions","text":""},{"location":"api/nn/#tklearn.nn.utils.collections.TensorDict.detach","title":"<code>detach()</code>","text":"<p>Detach all tensors.</p> Source code in <code>src/tklearn/nn/utils/collections.py</code> <pre><code>def detach(self) -&gt; TensorDict[T]:\n    \"\"\"Detach all tensors.\"\"\"\n    c = {}\n    for key, value in self.items():\n        if isinstance(value, torch.Tensor):\n            value = value.detach()\n        c[key] = value\n    return TensorDict(c)\n</code></pre>"},{"location":"api/nn/#tklearn.nn.utils.collections.TensorDict.to","title":"<code>to(device, non_blocking=False)</code>","text":"<p>Move all tensors to device.</p> Source code in <code>src/tklearn/nn/utils/collections.py</code> <pre><code>def to(\n    self, device: Union[torch.device, str], non_blocking: bool = False\n) -&gt; TensorDict[T]:\n    \"\"\"Move all tensors to device.\"\"\"\n    c = {}\n    for key, value in self.items():\n        if isinstance(value, torch.Tensor):\n            value = value.to(device, non_blocking=non_blocking)\n        c[key] = value\n    return TensorDict(c)\n</code></pre>"},{"location":"api/nn/#tklearn.nn.utils.collections.TensorDict.cuda","title":"<code>cuda(non_blocking=False)</code>","text":"<p>Move all tensors to cuda.</p> Source code in <code>src/tklearn/nn/utils/collections.py</code> <pre><code>def cuda(self, non_blocking: bool = False) -&gt; TensorDict[T]:\n    \"\"\"Move all tensors to cuda.\"\"\"\n    return self.to(\"cuda\", non_blocking=non_blocking)\n</code></pre>"},{"location":"api/nn/#tklearn.nn.utils.collections.TensorDict.cpu","title":"<code>cpu()</code>","text":"<p>Move all tensors to cpu.</p> Source code in <code>src/tklearn/nn/utils/collections.py</code> <pre><code>def cpu(self) -&gt; TensorDict[T]:\n    \"\"\"Move all tensors to cpu.\"\"\"\n    return self.to(\"cpu\")\n</code></pre>"},{"location":"api/nn/#tklearn.nn.utils.collections.TensorDict.item","title":"<code>item()</code>","text":"<p>Convert all tensors to float.</p> Source code in <code>src/tklearn/nn/utils/collections.py</code> <pre><code>def item(self) -&gt; TensorDict[float]:\n    \"\"\"Convert all tensors to float.\"\"\"\n    c = {}\n    for key, value in self.items():\n        if isinstance(value, torch.Tensor):\n            value = value.item()\n        c[key] = value\n    return TensorDict(c)\n</code></pre>"},{"location":"api/nn/#devices","title":"Devices","text":""},{"location":"api/nn/#tklearn.nn.utils.devices","title":"<code>devices</code>","text":""},{"location":"api/nn/#preprocessing","title":"Preprocessing","text":""},{"location":"api/nn/#tklearn.nn.utils.preprocessing","title":"<code>preprocessing</code>","text":""},{"location":"api/plotting/","title":"Plotting","text":"<p>Visualization utilities for text analysis and model interpretability.</p>"},{"location":"api/plotting/#embeddings-visualization","title":"Embeddings Visualization","text":""},{"location":"api/plotting/#tklearn.plotting.embeddings","title":"<code>embeddings</code>","text":""},{"location":"api/plotting/#tklearn.plotting.embeddings-functions","title":"Functions","text":""},{"location":"api/plotting/#tklearn.plotting.embeddings.embed2d","title":"<code>embed2d(X, dim_reducer='umap')</code>","text":"<p>Perform 2D embedding using UMAP or t-SNE.</p> <p>Defaults to 'umap'. Falls back to 'tsne' if 'umap' is requested but not installed.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>ndarray</code> <p>The high-dimensional data array (n_samples, n_features).</p> required <code>dim_reducer</code> <code>str</code> <p>The dimensionality reduction technique to use ('umap' or 'tsne'). Defaults to 'umap'.</p> <code>'umap'</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>The 2D embedded data array (n_samples, 2).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If an unsupported embedder is specified.</p> Source code in <code>src/tklearn/plotting/embeddings.py</code> <pre><code>def embed2d(X: np.ndarray, dim_reducer: str = \"umap\") -&gt; np.ndarray:  # noqa: N803 The capital X is conventional here\n    \"\"\"Perform 2D embedding using UMAP or t-SNE.\n\n    Defaults to 'umap'. Falls back to 'tsne' if 'umap'\n    is requested but not installed.\n\n    Parameters\n    ----------\n    X : np.ndarray\n        The high-dimensional data array (n_samples, n_features).\n    dim_reducer : str, optional\n        The dimensionality reduction technique to use ('umap' or 'tsne').\n        Defaults to 'umap'.\n\n    Returns\n    -------\n    np.ndarray\n        The 2D embedded data array (n_samples, 2).\n\n    Raises\n    ------\n    ValueError\n        If an unsupported embedder is specified.\n    \"\"\"\n    dim_reducer = dim_reducer.lower()  # Convert embedder name to lowercase\n    if dim_reducer == \"umap\" and UMAP is not None:\n        # Use UMAP if available and requested\n        # supress warnings from UMAP\n        with warnings.catch_warnings():\n            warnings.simplefilter(\"ignore\")\n            # Perform UMAP embedding\n            umap = UMAP(n_components=2, random_state=42)\n            # Added random_state for reproducibility\n            X_embedded = umap.fit_transform(X)\n    elif dim_reducer in {\"tsne\", \"t-sne\", \"umap\"}:\n        # Use t-SNE if requested, or if UMAP was requested but not installed\n        if dim_reducer == \"umap\":\n            msg = \"umap-learn is not installed, falling back to scikit-learn's t-SNE\"\n            warnings.warn(msg, stacklevel=1)\n        # Use t-SNE\n        X_embedded = TSNE(\n            n_components=2,\n            init=\"pca\",\n            learning_rate=\"auto\",\n            random_state=42,\n            n_jobs=1,\n        ).fit_transform(X)  # Added common parameters\n    else:\n        # Raise error for unsupported embedder\n        msg = (\n            f\"embedder '{dim_reducer}' not supported. Choose 'umap' or 't-sne'\"\n        )\n        raise ValueError(msg)\n    return X_embedded\n</code></pre>"},{"location":"api/plotting/#tklearn.plotting.embeddings.plot_embedding","title":"<code>plot_embedding(data, x='embedding', y='label', style='seaborn', cmap='rainbow', alpha=0.5, figsize=(8, 6), dim_reducer='umap', dpi=100, legend_max_ncols=5)</code>","text":"<p>Generate a 2D scatter plot of embedded data, colored by labels.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DataFrame</code> <p>DataFrame containing the data. Must have columns specified by <code>x</code> (containing lists/arrays of embeddings) and <code>y</code> (containing labels).</p> required <code>x</code> <code>str</code> <p>Name of the column containing the high-dimensional embeddings. Defaults to \"embedding\".</p> <code>'embedding'</code> <code>y</code> <code>str</code> <p>Name of the column containing the labels for coloring. Defaults to \"label\".</p> <code>'label'</code> <code>style</code> <code>Any</code> <p>Matplotlib style to use for the plot (e.g., 'seaborn', 'ggplot'). Defaults to \"seaborn\".</p> <code>'seaborn'</code> <code>cmap</code> <code>Any</code> <p>Colormap to use for coloring points by label. Defaults to \"rainbow\".</p> <code>'rainbow'</code> <code>alpha</code> <code>float</code> <p>Transparency level for the scatter points (0=transparent, 1=opaque). Defaults to 0.5.</p> <code>0.5</code> <code>figsize</code> <code>Tuple[int, int]</code> <p>Tuple specifying the figure size (width, height) in inches. Defaults to (8, 6).</p> <code>(8, 6)</code> <code>dim_reducer</code> <code>str</code> <p>Dimensionality reduction technique ('umap' or 'tsne'). Defaults to \"umap\".</p> <code>'umap'</code> <code>dpi</code> <code>float</code> <p>Dots per inch (resolution) for the figure. Defaults to 100.</p> <code>100</code> <code>legend_max_ncols</code> <code>int</code> <p>Maximum number of columns to use for the legend if it needs to be rearranged due to height. Defaults to 5.</p> <code>5</code> <p>Returns:</p> Type Description <code>Figure</code> <p>The Matplotlib Figure object containing the plot.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If columns specified by <code>x</code> or <code>y</code> are not found in the DataFrame, or if embedding vectors in column <code>x</code> cannot be stacked (e.g., due to inconsistent dimensions).</p> <code>TypeError</code> <p>If the column specified by <code>x</code> does not contain iterable objects (like lists or numpy arrays).</p> Source code in <code>src/tklearn/plotting/embeddings.py</code> <pre><code>def plot_embedding(\n    data: Union[pd.DataFrame, Dataset],\n    x: str = \"embedding\",\n    y: str = \"label\",\n    style: Any = \"seaborn\",\n    cmap: Any = \"rainbow\",\n    alpha: float = 0.5,\n    figsize: tuple[int, int] = (8, 6),  # Adjusted default figsize\n    dim_reducer: str = \"umap\",\n    dpi: float = 100,\n    legend_max_ncols: int = 5,\n) -&gt; plt.Figure:\n    \"\"\"Generate a 2D scatter plot of embedded data, colored by labels.\n\n    Parameters\n    ----------\n    data : pd.DataFrame\n        DataFrame containing the data. Must have columns specified by\n        `x` (containing lists/arrays of embeddings) and `y` (containing labels).\n    x : str, optional\n        Name of the column containing the high-dimensional embeddings.\n        Defaults to \"embedding\".\n    y : str, optional\n        Name of the column containing the labels for coloring.\n        Defaults to \"label\".\n    style : Any, optional\n        Matplotlib style to use for the plot (e.g., 'seaborn', 'ggplot').\n        Defaults to \"seaborn\".\n    cmap : Any, optional\n        Colormap to use for coloring points by label. Defaults to \"rainbow\".\n    alpha : float, optional\n        Transparency level for the scatter points (0=transparent, 1=opaque).\n        Defaults to 0.5.\n    figsize : Tuple[int, int], optional\n        Tuple specifying the figure size (width, height) in inches.\n        Defaults to (8, 6).\n    dim_reducer : str, optional\n        Dimensionality reduction technique ('umap' or 'tsne').\n        Defaults to \"umap\".\n    dpi : float, optional\n        Dots per inch (resolution) for the figure. Defaults to 100.\n    legend_max_ncols : int, optional\n        Maximum number of columns to use for the legend if it needs\n        to be rearranged due to height. Defaults to 5.\n\n    Returns\n    -------\n    plt.Figure\n        The Matplotlib Figure object containing the plot.\n\n    Raises\n    ------\n    ValueError\n        If columns specified by `x` or `y` are not found in the DataFrame,\n        or if embedding vectors in column `x` cannot be stacked (e.g., due\n        to inconsistent dimensions).\n    TypeError\n        If the column specified by `x` does not contain iterable objects\n        (like lists or numpy arrays).\n    \"\"\"\n    x_col, y_col = x, y  # Assign column names\n\n    # --- Input Validation ---\n    if isinstance(data, Dataset):\n        # Convert Dataset to DataFrame if needed\n        data = data.select_columns([x_col, y_col]).to_pandas()\n    elif x_col not in data.columns:\n        msg = f\"column '{x_col}' not found in DataFrame.\"\n        raise ValueError(msg)\n    elif y_col not in data.columns:\n        err = f\"column '{y_col}' not found in DataFrame.\"\n        raise ValueError(err)\n\n    if not data[x_col].apply(lambda item: hasattr(item, \"__iter__\")).all():\n        err = f\"column '{x_col}' must contain iterable objects\"\n        raise TypeError(err)\n\n    # Convert embedding column to a NumPy array\n    # Use np.vstack for robust handling of list/array types\n    try:\n        X_high_dim = np.vstack(data[x_col].values)\n    except ValueError as e:\n        raise ValueError(\n            f\"Could not stack embedding vectors from column '{x_col}'. Ensure all embeddings have the same dimension.\"\n        ) from e\n\n    # Perform 2D embedding\n    X_embedded = embed2d(X_high_dim, dim_reducer=dim_reducer)\n\n    # Prepare labels as categorical data\n    labels = pd.Series(data[y_col]).astype(\"category\")\n\n    # Get the actual style name and colormap object\n    style_name = get_style(style)\n    if style_name is None:\n        warnings.warn(\n            f\"Style '{style}' not found. Using default Matplotlib style.\",\n            stacklevel=1,\n        )\n        # Use default if not found\n        style_name = plt.style.use(\"default\")\n\n    try:\n        cmap_obj = plt.colormaps.get_cmap(cmap)\n    except ValueError:\n        warnings.warn(\n            f\"Colormap '{cmap}' not found. Using 'viridis'.\", stacklevel=1\n        )\n        # Use default if not found\n        cmap_obj = plt.colormaps.get_cmap(\"viridis\")\n\n    num_classes = len(labels.cat.categories)  # Number of unique labels\n\n    # Create the plot using the specified style\n    with plt.style.context(style=style_name):\n        # Create figure and axes\n        # Removed tight_layout=True initially, will apply later\n        fig, ax = plt.subplots(figsize=figsize)\n        fig.set_dpi(dpi)\n\n        handles = []  # List to store handles for the legend\n        # Iterate through each unique label to plot its points\n        for label_id, label in enumerate(labels.cat.categories):\n            # Find indices for the current label\n            idx = np.where(labels == label)[0]\n            coords = X_embedded[idx]  # Get the 2D coordinates for these points\n            color = cmap_obj(\n                label_id / (num_classes - 1) if num_classes &gt; 1 else 0.5\n            )  # Get color from colormap\n\n            # Create the scatter plot for the current label\n            scatter = ax.scatter(\n                x=coords[:, 0],  # x-coordinates\n                y=coords[:, 1],  # y-coordinates\n                color=color,\n                label=str(label),  # Ensure label is a string for legend\n                alpha=alpha,\n                edgecolors=\"none\",  # No borders around points\n            )\n            handles.append(scatter)  # Add the scatter plot artist to handles\n\n        # Remove default legend if it exists (we create a custom one)\n        if ax.get_legend():\n            ax.get_legend().remove()\n\n        # Add the custom legend outside the plot\n        legend = set_legend(  # Capture the returned legend object\n            handles,\n            fig,\n            ax,\n            title=str(y_col),  # Ensure title is a string\n            max_ncols=legend_max_ncols,\n        )\n\n        # Add grid lines\n        ax.grid(True)\n        # Set plot titles and labels (optional, but good practice)\n        ax.set_title(\n            f\"{dim_reducer.upper()} Embedding of {x_col} colored by {y_col}\"\n        )\n        ax.set_xlabel(f\"{dim_reducer.upper()} Dimension 1\")\n        ax.set_ylabel(f\"{dim_reducer.upper()} Dimension 2\")\n\n        # Apply tight layout at the end to adjust spacing\n        try:\n            # Adjust rect to try and prevent legend overlap\n            # The right boundary is adjusted based on estimated legend width factor\n            legend_width_factor = legend.get_window_extent(\n                renderer=get_renderer(fig)\n            ).width / (fig.dpi * figsize[0])\n            right_boundary = max(\n                0.8, 1.0 - legend_width_factor * 1.1\n            )  # Ensure some minimum plot area\n            fig.tight_layout(rect=[0, 0, right_boundary, 1])\n        except (ValueError, AttributeError):\n            # Catch potential errors during layout\n            # Sometimes tight_layout fails, especially with complex legends or backends\n            warnings.warn(\n                \"fig.tight_layout() failed. Plot spacing might not be optimal.\",\n                stacklevel=1,\n            )\n\n    return fig  # Return the figure object\n</code></pre>"},{"location":"api/plotting/#helpers","title":"Helpers","text":""},{"location":"api/plotting/#tklearn.plotting.helpers","title":"<code>helpers</code>","text":""},{"location":"api/plotting/#tklearn.plotting.helpers-functions","title":"Functions","text":""},{"location":"api/plotting/#tklearn.plotting.helpers.get_style","title":"<code>get_style(style)</code>","text":"<p>Retrieve the actual Matplotlib style name based on the input string.</p> <p>Parameters:</p> Name Type Description Default <code>style</code> <code>str</code> <p>The desired style name (e.g., \"seaborn\", \"ggplot\").</p> required <p>Returns:</p> Type Description <code>str or None</code> <p>The corresponding Matplotlib style name if found, otherwise None.</p> Source code in <code>src/tklearn/plotting/helpers.py</code> <pre><code>def get_style(style: str) -&gt; str | None:\n    \"\"\"Retrieve the actual Matplotlib style name based on the input string.\n\n    Parameters\n    ----------\n    style : str\n        The desired style name (e.g., \"seaborn\", \"ggplot\").\n\n    Returns\n    -------\n    str or None\n        The corresponding Matplotlib style name if found, otherwise None.\n    \"\"\"\n    return available_styles.get(style)\n</code></pre>"},{"location":"api/plotting/#tklearn.plotting.helpers.get_renderer","title":"<code>get_renderer(fig)</code>","text":"<p>Get the renderer instance for a Matplotlib figure.</p> <p>Needed for accurately calculating bounding boxes of elements like legends.</p> <p>Parameters:</p> Name Type Description Default <code>fig</code> <code>Figure</code> <p>The Matplotlib figure instance.</p> required <p>Returns:</p> Type Description <code>RendererBase</code> <p>The renderer instance.</p> <p>Raises:</p> Type Description <code>AttributeError</code> <p>If a renderer cannot be found for the figure's backend.</p> Source code in <code>src/tklearn/plotting/helpers.py</code> <pre><code>def get_renderer(fig: plt.Figure) -&gt; RendererBase:\n    \"\"\"Get the renderer instance for a Matplotlib figure.\n\n    Needed for accurately calculating bounding boxes of elements like legends.\n\n    Parameters\n    ----------\n    fig : plt.Figure\n        The Matplotlib figure instance.\n\n    Returns\n    -------\n    RendererBase\n        The renderer instance.\n\n    Raises\n    ------\n    AttributeError\n        If a renderer cannot be found for the figure's backend.\n    \"\"\"\n    # Try different ways to get the renderer depending on Matplotlib version/backend\n    if hasattr(fig.canvas, \"get_renderer\"):\n        return fig.canvas.get_renderer()\n    # Deprecated way, kept for compatibility\n    elif hasattr(fig, \"_get_renderer\"):  # pragma: no cover\n        return fig._get_renderer()\n    # If no renderer found, raise an error\n    backend = matplotlib.get_backend()\n    msg = f\"Could not find a renderer for the '{backend}' backend.\"\n    raise AttributeError(msg)\n</code></pre>"},{"location":"api/plotting/#tklearn.plotting.helpers.set_legend","title":"<code>set_legend(handles, fig, ax, title, frameon=True, fancybox=True, ncols=1, max_ncols=None)</code>","text":"<p>Create and position a legend outside the plot area.</p> <p>Adjusts the number of columns dynamically to fit the available space if <code>max_ncols</code> is specified.</p> <p>Parameters:</p> Name Type Description Default <code>handles</code> <code>list[Artist]</code> <p>A list of Matplotlib artists (like scatter points) to include in the legend.</p> required <code>fig</code> <code>Figure</code> <p>The Matplotlib figure.</p> required <code>ax</code> <code>Axes</code> <p>The Matplotlib axes where the plot resides.</p> required <code>title</code> <code>str</code> <p>The title for the legend.</p> required <code>frameon</code> <code>bool</code> <p>Whether to draw a frame around the legend. Defaults to True.</p> <code>True</code> <code>fancybox</code> <code>bool</code> <p>Whether to use rounded corners for the legend frame. Defaults to True.</p> <code>True</code> <code>ncols</code> <code>int</code> <p>Initial number of columns for the legend. Defaults to 1.</p> <code>1</code> <code>max_ncols</code> <code>int or None</code> <p>Maximum number of columns allowed for the legend. If None, no dynamic adjustment is made. Defaults to None.</p> <code>None</code> <p>Returns:</p> Type Description <code>Legend</code> <p>The created Matplotlib legend object.</p> Source code in <code>src/tklearn/plotting/helpers.py</code> <pre><code>def set_legend(\n    handles: list[Artist],\n    fig: plt.Figure,\n    ax: plt.Axes,\n    title: str,\n    frameon: bool = True,\n    fancybox: bool = True,\n    ncols: int = 1,\n    max_ncols: int | None = None,\n) -&gt; Legend:\n    \"\"\"Create and position a legend outside the plot area.\n\n    Adjusts the number of columns dynamically to fit the available space\n    if `max_ncols` is specified.\n\n    Parameters\n    ----------\n    handles : list[Artist]\n        A list of Matplotlib artists (like scatter points) to include in the legend.\n    fig : plt.Figure\n        The Matplotlib figure.\n    ax : plt.Axes\n        The Matplotlib axes where the plot resides.\n    title : str\n        The title for the legend.\n    frameon : bool, optional\n        Whether to draw a frame around the legend. Defaults to True.\n    fancybox : bool, optional\n        Whether to use rounded corners for the legend frame. Defaults to True.\n    ncols : int, optional\n        Initial number of columns for the legend. Defaults to 1.\n    max_ncols : int or None, optional\n        Maximum number of columns allowed for the legend. If None,\n        no dynamic adjustment is made. Defaults to None.\n\n    Returns\n    -------\n    matplotlib.legend.Legend\n        The created Matplotlib legend object.\n    \"\"\"\n    loc = \"upper left\"  # Initial location anchor for the legend\n    # Position relative to the axes (outside, top right)\n    bbox_to_anchor = (1.01, 1.01)\n\n    renderer = get_renderer(fig)  # Get the renderer to calculate sizes\n\n    # Get plot dimensions\n    plot_extent = ax.get_tightbbox(renderer)\n    plot_height = plot_extent.height / fig.dpi\n    plot_width = plot_extent.width / fig.dpi\n\n    # Extract labels from handles\n    labels = [h.get_label() for h in handles]\n\n    # Create the initial legend\n    legend = ax.legend(\n        handles,\n        labels,\n        title=title,\n        frameon=frameon,\n        fancybox=fancybox,\n        loc=loc,\n        bbox_to_anchor=bbox_to_anchor,\n        ncols=ncols,\n    )\n\n    # Calculate initial legend size\n    legend_extent = legend.get_tightbbox(renderer)\n    legend_height = legend_extent.height / fig.dpi\n    legend_width = legend_extent.width / fig.dpi\n\n    # Adjust figure size to accommodate the legend\n    # Note: This might need fine-tuning depending on the desired layout\n    fig.set_size_inches(\n        plot_width + legend_width * 1.1,\n        max(plot_height, legend_height),\n        forward=True,\n    )  # Added buffer * 1.1\n    # Ensure tight layout is applied *after* resizing\n    try:\n        fig.tight_layout()\n    except (\n        ValueError\n    ):  # Sometimes tight_layout fails after resizing, ignore for now\n        pass\n    # Re-position legend slightly after potential layout adjustments\n    legend.set_bbox_to_anchor(bbox_to_anchor)\n\n    # If max_ncols is set, try increasing columns if legend is too tall\n    if max_ncols is None:\n        return legend\n\n    current_ncols = ncols\n    # Loop to increase columns if legend is taller than plot (approximate check)\n    # And we haven't reached the maximum allowed columns\n    while (current_ncols &lt; max_ncols) and (\n        legend_height &gt; plot_height * 1.1\n    ):  # Added buffer * 1.1\n        current_ncols += 1\n        # Remove the old legend\n        legend.remove()\n        # Store previous figure size\n        prev_width, prev_height = fig.get_size_inches()\n\n        # Create new legend with more columns\n        legend = ax.legend(\n            handles,\n            labels,\n            title=title,\n            frameon=frameon,\n            fancybox=fancybox,\n            loc=loc,\n            bbox_to_anchor=bbox_to_anchor,\n            ncols=current_ncols,\n        )\n\n        # Recalculate legend size\n        legend_extent = legend.get_tightbbox(renderer)\n        legend_height = legend_extent.height / fig.dpi\n        legend_width = legend_extent.width / fig.dpi\n\n        # Adjust figure size again\n        # Use the original plot width and the new legend width\n        new_fig_width = plot_width + legend_width * 1.1\n        new_fig_height = max(plot_height, legend_height)\n        fig.set_size_inches(new_fig_width, new_fig_height, forward=True)\n        try:\n            fig.tight_layout()\n        except ValueError:\n            pass\n        legend.set_bbox_to_anchor(bbox_to_anchor)\n\n        # Break if increasing columns didn't significantly reduce height (or made it worse)\n        # This prevents potential infinite loops in edge cases\n        if (\n            current_ncols &gt; ncols + 1\n            and legend_height\n            &gt;= legend.get_tightbbox(renderer).height / fig.dpi * 0.95\n        ):  # Check if height reduction is minimal\n            warnings.warn(\n                \"Legend height not reducing significantly with more columns. Stopping column increase.\",\n                stacklevel=1,\n            )\n            break\n\n    return legend\n</code></pre>"},{"location":"api/utils/","title":"Utils","text":"<p>General utility functions for text processing and data manipulation.</p>"},{"location":"api/utils/#array-utilities","title":"Array Utilities","text":""},{"location":"api/utils/#tklearn.utils.array","title":"<code>array</code>","text":"<p>Utilities for working with arrays and tensors.</p>"},{"location":"api/utils/#tklearn.utils.array-classes","title":"Classes","text":""},{"location":"api/utils/#tklearn.utils.array.MovableToDeviceMixin","title":"<code>MovableToDeviceMixin</code>","text":"<p>Mixin class for moving an object to a device.</p>"},{"location":"api/utils/#tklearn.utils.array.MovableToDeviceMixin-functions","title":"Functions","text":""},{"location":"api/utils/#tklearn.utils.array.MovableToDeviceMixin.to","title":"<code>to(device, non_blocking=False)</code>","text":"<p>Move the object to a device.</p> <p>Parameters:</p> Name Type Description Default <code>device</code> <code>Union[str, device]</code> <p>The device to move the object to.</p> required <code>non_blocking</code> <code>bool</code> <p>Whether to move the object asynchronously, by default False.</p> <code>False</code> <p>Returns:</p> Name Type Description <code>output</code> <code>MovableToDevice</code> <p>The object moved to the device.</p> Source code in <code>src/tklearn/utils/array.py</code> <pre><code>def to(\n    self,\n    device: Union[str, torch.device],\n    non_blocking: bool = False,\n) -&gt; Self:\n    \"\"\"Move the object to a device.\n\n    Parameters\n    ----------\n    device : Union[str, torch.device]\n        The device to move the object to.\n    non_blocking : bool, optional\n        Whether to move the object asynchronously, by default False.\n\n    Returns\n    -------\n    output : MovableToDevice\n        The object moved to the device.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"api/utils/#tklearn.utils.array-functions","title":"Functions","text":""},{"location":"api/utils/#tklearn.utils.array.to_numpy","title":"<code>to_numpy(obj, *args)</code>","text":"<p>Convert an object to a numpy array.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>object</code> <p>The object to convert to a numpy array.</p> required <p>Returns:</p> Name Type Description <code>output</code> <code>Any</code> <p>The object converted to a numpy array.</p> Source code in <code>src/tklearn/utils/array.py</code> <pre><code>def to_numpy(obj: object, *args) -&gt; RT:\n    \"\"\"Convert an object to a numpy array.\n\n    Parameters\n    ----------\n    obj : object\n        The object to convert to a numpy array.\n\n    Returns\n    -------\n    output : Any\n        The object converted to a numpy array.\n    \"\"\"\n    if len(args) &gt; 0:\n        return (to_numpy(obj), *map(to_numpy, args))\n    if isinstance(obj, torch.Tensor):\n        return obj.detach().cpu().numpy()\n    elif isinstance(obj, Dict):\n        return {k: to_numpy(v) for k, v in obj.items()}\n    elif isinstance(obj, Tuple) and hasattr(obj, \"_fields\"):\n        # namedtuple\n        dtype = type(obj)\n        return dtype(to_numpy(v) for v in obj)\n    elif isinstance(obj, Tuple):\n        return tuple(to_numpy(v) for v in obj)\n    return np.array(obj)\n</code></pre>"},{"location":"api/utils/#tklearn.utils.array.to_torch","title":"<code>to_torch(obj)</code>","text":"<p>Convert an object to a torch tensor.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>object</code> <p>The object to convert to a torch tensor.</p> required <p>Returns:</p> Name Type Description <code>output</code> <code>Any</code> <p>The object converted to a torch tensor.</p> Source code in <code>src/tklearn/utils/array.py</code> <pre><code>def to_torch(obj: object) -&gt; RT:\n    \"\"\"Convert an object to a torch tensor.\n\n    Parameters\n    ----------\n    obj : object\n        The object to convert to a torch tensor.\n\n    Returns\n    -------\n    output : Any\n        The object converted to a torch tensor.\n    \"\"\"\n    if isinstance(obj, Dict):\n        return {k: to_torch(v) for k, v in obj.items()}\n    elif isinstance(obj, Tuple) and hasattr(obj, \"_fields\"):\n        # namedtuple\n        dtype = type(obj)\n        return dtype(to_torch(v) for v in obj)\n    elif isinstance(obj, Tuple) and hasattr(obj, \"_fields\"):\n        # namedtuple\n        dtype = type(obj)\n        return dtype(to_torch(v) for v in obj)\n    elif isinstance(obj, Tuple):\n        return tuple(to_torch(v) for v in obj)\n    if not isinstance(obj, np.ndarray):\n        obj = np.array(obj)\n    return torch.from_numpy(obj)\n</code></pre>"},{"location":"api/utils/#tklearn.utils.array.detach","title":"<code>detach(obj)</code>","text":"<p>Detach a torch tensor from its computation graph.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>T</code> <p>The object to detach from its computation graph.</p> required <p>Returns:</p> Name Type Description <code>output</code> <code>T</code> <p>The object detached from its computation graph.</p> Source code in <code>src/tklearn/utils/array.py</code> <pre><code>def detach(obj: T) -&gt; T:\n    \"\"\"Detach a torch tensor from its computation graph.\n\n    Parameters\n    ----------\n    obj : T\n        The object to detach from its computation graph.\n\n    Returns\n    -------\n    output : T\n        The object detached from its computation graph.\n    \"\"\"\n    if isinstance(obj, torch.Tensor):\n        return obj.detach()\n    elif isinstance(obj, Dict):\n        return {k: detach(v) for k, v in obj.items()}\n    elif isinstance(obj, List):\n        return [detach(v) for v in obj]\n    elif isinstance(obj, Tuple) and hasattr(obj, \"_fields\"):\n        # namedtuple\n        dtype = type(obj)\n        return dtype(detach(v) for v in obj)\n    elif isinstance(obj, Tuple):\n        return tuple(detach(v) for v in obj)\n    return obj\n</code></pre>"},{"location":"api/utils/#tklearn.utils.array.move_to_device","title":"<code>move_to_device(obj, device, non_blocking=False)</code>","text":"<p>Move an object to a device.</p> <p>If the object is a torch tensor or module with the provided device, it is returned as is. If the object is a mapping, list, or tuple, the function is applied recursively to each element in the object.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>object</code> <p>The object to move to the device.</p> required <code>device</code> <code>Union[str, device]</code> <p>The device to move the object to.</p> required <p>Returns:</p> Name Type Description <code>output</code> <code>object</code> <p>The object moved to the device.</p> Source code in <code>src/tklearn/utils/array.py</code> <pre><code>def move_to_device(\n    obj: T, device: Union[str, torch.device], non_blocking: bool = False\n) -&gt; T:\n    \"\"\"Move an object to a device.\n\n    If the object is a torch tensor or module with the provided device, it is\n    returned as is. If the object is a mapping, list, or tuple, the function is\n    applied recursively to each element in the object.\n\n    Parameters\n    ----------\n    obj : object\n        The object to move to the device.\n    device : Union[str, torch.device]\n        The device to move the object to.\n\n    Returns\n    -------\n    output : object\n        The object moved to the device.\n    \"\"\"\n    if isinstance(obj, (torch.nn.Module, torch.Tensor, MovableToDeviceMixin)):\n        return obj.to(device, non_blocking=non_blocking)\n    elif isinstance(obj, Mapping):\n        data = {}\n        for k, v in obj.items():\n            data[k] = move_to_device(v, device, non_blocking=non_blocking)\n        return data\n    elif isinstance(obj, Tuple) and hasattr(obj, \"_fields\"):\n        # namedtuple, e.g., RecordBatch\n        dtype = type(obj)\n        return dtype(\n            move_to_device(v, device, non_blocking=non_blocking) for v in obj\n        )\n    elif isinstance(obj, Tuple):\n        return tuple(\n            move_to_device(v, device, non_blocking=non_blocking) for v in obj\n        )\n    elif isinstance(obj, List):\n        return [\n            move_to_device(v, device, non_blocking=non_blocking) for v in obj\n        ]\n    elif isinstance(obj, str):\n        return obj\n    raise ValueError(\n        f\"cannot move object of type '{obj.__class__.__name__}' to device\"\n    )\n</code></pre>"},{"location":"api/utils/#tklearn.utils.array.concat","title":"<code>concat(objs, /, axis=0)</code>","text":"<p>Concatenate a list of objects along an axis.</p> <p>Parameters:</p> Name Type Description Default <code>objs</code> <code>List[RT]</code> <p>The list of objects to concatenate.</p> required <code>axis</code> <code>int</code> <p>The axis to concatenate along, by default 0.</p> <code>0</code> <p>Returns:</p> Name Type Description <code>output</code> <code>RT</code> <p>The concatenated objects.</p> Source code in <code>src/tklearn/utils/array.py</code> <pre><code>def concat(objs: List[RT], /, axis: int = 0) -&gt; RT:\n    \"\"\"Concatenate a list of objects along an axis.\n\n    Parameters\n    ----------\n    objs : List[RT]\n        The list of objects to concatenate.\n    axis : int, optional\n        The axis to concatenate along, by default 0.\n\n    Returns\n    -------\n    output : RT\n        The concatenated objects.\n    \"\"\"\n    if len(objs) == 0:\n        msg = \"cannot concatenate empty list of objects\"\n        raise ValueError(msg)\n    # remove None objects from list\n    objs = [o for o in objs if o is not None]\n    if len(objs) == 0:\n        # if all objects were None\n        return None\n    elem = objs[0]\n    if len(objs) == 1:\n        return elem\n    if isinstance(elem, Mapping):\n        keys = set(elem.keys())\n        output = {k: concat([o[k] for o in objs], axis=axis) for k in keys}\n        try:\n            # try to convert to original type assuming dict style constructor\n            return type(elem)(**output)\n        except TypeError:\n            # if not possible, return as a dictionary\n            return output\n    if isinstance(elem, Tuple) and hasattr(elem, \"_fields\"):  # namedtuple\n        dtype = type(elem)\n        size = len(elem)\n        # difference with normal tuple is that we return the same type as the input\n        return dtype(\n            concat([o[i] for o in objs], axis=axis) for i in range(size)\n        )\n    if isinstance(elem, Tuple):\n        size = len(elem)\n        return tuple(\n            concat([o[i] for o in objs], axis=axis) for i in range(size)\n        )\n    if isinstance(elem, np.ndarray):\n        return np.concatenate(objs, axis=axis)\n    if isinstance(elem, torch.Tensor):\n        return torch.cat(objs, dim=axis)\n    if isinstance(elem, List):\n        return functools.reduce(operator.iadd, objs, [])\n    msg = f\"cannot concatenate objects of type '{elem.__class__.__name__}'\"\n    raise ValueError(msg)\n</code></pre>"},{"location":"api/utils/#tklearn.utils.array.length_of_first_array_like_in_nested_dict","title":"<code>length_of_first_array_like_in_nested_dict(data)</code>","text":"<p>Return the length of the first list in a nested dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Dict</code> <p>The nested dictionary to search for the first list.</p> required <p>Returns:</p> Name Type Description <code>length</code> <code>int</code> <p>The length of the first list in the nested dictionary.</p> Source code in <code>src/tklearn/utils/array.py</code> <pre><code>def length_of_first_array_like_in_nested_dict(data: Dict) -&gt; int:\n    \"\"\"Return the length of the first list in a nested dictionary.\n\n    Parameters\n    ----------\n    data : Dict\n        The nested dictionary to search for the first list.\n\n    Returns\n    -------\n    length : int\n        The length of the first list in the nested dictionary.\n    \"\"\"\n    if isinstance(data, (HuggingFaceDataset, ListLike)):\n        return len(data)\n    if isinstance(data, (np.ndarray, pd.DataFrame)):\n        return data.shape[0]\n    if isinstance(data, torch.Tensor):\n        return data.size(0)\n    if isinstance(data, Tuple):\n        # namedtuple check =&gt; hasattr(data, \"_fields\")\n        # tuple or namedtuple\n        for v in data:\n            return length_of_first_array_like_in_nested_dict(v)\n        return 0\n    if isinstance(data, (Sequence, pd.Series)) and not isinstance(data, str):\n        # any other sequence\n        return len(data)\n    if isinstance(data, Mapping):\n        for v in data.values():\n            return length_of_first_array_like_in_nested_dict(v)\n        return 0\n    # otherwise, it should be a dict\n    msg = f\"expected data type array-like or dict, got {type(data).__name__}\"\n    raise TypeError(msg)\n</code></pre>"},{"location":"api/utils/#tklearn.utils.array.get_index","title":"<code>get_index(data, index)</code>","text":"<pre><code>get_index(data: List[T], index: int) -&gt; T\n</code></pre><pre><code>get_index(data: List[T], index: slice) -&gt; List[T]\n</code></pre><pre><code>get_index(data: pd.DataFrame, index: int) -&gt; pd.Series\n</code></pre><pre><code>get_index(data: pd.DataFrame, index: slice) -&gt; pd.DataFrame\n</code></pre><pre><code>get_index(data: pd.Series, index: int) -&gt; Any\n</code></pre><pre><code>get_index(data: pd.Series, index: slice) -&gt; pd.Series\n</code></pre> <p>Get the value at a specific index in a nested dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Any</code> <p>The nested dictionary to search for the value at the index.</p> required <code>index</code> <code>int | slice</code> <p>The index to search for in the nested dictionary.</p> required <p>Returns:</p> Name Type Description <code>value</code> <code>Any</code> <p>The value at the index in the nested dictionary.</p> Source code in <code>src/tklearn/utils/array.py</code> <pre><code>def get_index(data: Any, index: Union[int, slice]) -&gt; Any:\n    \"\"\"Get the value at a specific index in a nested dictionary.\n\n    Parameters\n    ----------\n    data : Any\n        The nested dictionary to search for the value at the index.\n    index : int | slice\n        The index to search for in the nested dictionary.\n\n    Returns\n    -------\n    value : Any\n        The value at the index in the nested dictionary.\n    \"\"\"\n    if isinstance(\n        data, (torch.Tensor, np.ndarray, HuggingFaceDataset, ListLike)\n    ):\n        # will return dtyped value\n        return data[index]\n    if isinstance(data, (pd.DataFrame, pd.Series)):\n        # will return dtyped value\n        return data.iloc[index]\n    if isinstance(data, Mapping):\n        return {k: get_index(v, index=index) for k, v in data.items()}\n    if isinstance(data, Tuple) and hasattr(data, \"_fields\"):\n        # namedtuple\n        dtype = type(data)\n        return dtype(*(get_index(item, index=index) for item in data))\n    if isinstance(data, Tuple):\n        return tuple(get_index(item, index=index) for item in data)\n    if isinstance(data, Sequence) and not isinstance(data, str):\n        # will return dtyped value\n        return data[index]\n    msg = f\"cannot get index from object of type '{type(data).__name__}'\"\n    raise ValueError(msg)\n</code></pre>"},{"location":"api/utils/#tklearn.utils.array.batched","title":"<code>batched(data, batch_size)</code>","text":"<p>Yield batches of data from a list or dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Any</code> <p>The data to yield batches from.</p> required <code>batch_size</code> <code>int</code> <p>The size of each batch.</p> required <p>Yields:</p> Name Type Description <code>batch</code> <code>Any</code> <p>A batch of data from the input data.</p> Source code in <code>src/tklearn/utils/array.py</code> <pre><code>def batched(data: T, batch_size: int) -&gt; Generator[T, None, None]:\n    \"\"\"Yield batches of data from a list or dictionary.\n\n    Parameters\n    ----------\n    data : Any\n        The data to yield batches from.\n    batch_size : int\n        The size of each batch.\n\n    Yields\n    ------\n    batch : Any\n        A batch of data from the input data.\n    \"\"\"\n    for i in range(0, len(data), batch_size):\n        yield get_index(data, slice(i, i + batch_size))\n</code></pre>"},{"location":"api/utils/#caching","title":"Caching","text":""},{"location":"api/utils/#tklearn.utils.cache","title":"<code>cache</code>","text":""},{"location":"api/utils/#tklearn.utils.cache-classes","title":"Classes","text":""},{"location":"api/utils/#tklearn.utils.cache.FileCache","title":"<code>FileCache(*, cache_dir=None)</code>","text":"Source code in <code>src/tklearn/utils/cache.py</code> <pre><code>def __init__(self, *, cache_dir: str | Path | None = None):\n    if cache_dir is None:\n        cache_dir = config.cache_dir\n    self.cache_dir = cache_dir\n</code></pre>"},{"location":"api/utils/#tklearn.utils.cache.FileCache-functions","title":"Functions","text":""},{"location":"api/utils/#tklearn.utils.cache.FileCache.cleanup","title":"<code>cleanup(max_age_hours=24)</code>","text":"<p>Clean up temporary files older than max_age_hours. Returns number of files removed.</p> Source code in <code>src/tklearn/utils/cache.py</code> <pre><code>def cleanup(self, max_age_hours: int = 24) -&gt; int:\n    \"\"\"Clean up temporary files older than max_age_hours.\n    Returns number of files removed.\"\"\"\n    current_time = time.time()\n    cleaned = 0\n    # Find all .tmp files recursively\n    for tmp_file in Path(self.cache_dir).rglob(\"*.tmp\"):\n        try:\n            stats = tmp_file.stat()\n            # Only remove if:\n            # 1. File is old enough\n            # 2. File size is not changing (indicating active write)\n            if current_time - stats.st_mtime &gt; max_age_hours * 3600:\n                # Double check file size hasn't changed\n                time.sleep(0.1)  # Brief pause\n                new_stats = tmp_file.stat()\n                if (\n                    new_stats.st_size == stats.st_size\n                ):  # File is not being written to\n                    tmp_file.unlink()\n                    cleaned += 1\n        except OSError:\n            continue\n    return cleaned\n</code></pre>"},{"location":"api/utils/#constants","title":"Constants","text":""},{"location":"api/utils/#tklearn.utils.constants","title":"<code>constants</code>","text":""},{"location":"api/utils/#copy-utilities","title":"Copy Utilities","text":""},{"location":"api/utils/#tklearn.utils.copy","title":"<code>copy</code>","text":""},{"location":"api/utils/#tklearn.utils.copy-functions","title":"Functions","text":""},{"location":"api/utils/#tklearn.utils.copy.deepcopy","title":"<code>deepcopy(x, device=None)</code>","text":"<p>Deep copy a model.</p> Source code in <code>src/tklearn/utils/copy.py</code> <pre><code>def deepcopy(x: T, device: Union[str, torch.device, None] = None) -&gt; T:\n    \"\"\"Deep copy a model.\"\"\"\n    try:\n        current_device = _get_device(x)\n    except TypeError:\n        return copy.deepcopy(x)\n    if device is None:\n        device = current_device\n    elif isinstance(device, str):\n        device = torch.device(device)\n    if device != current_device:\n        # if x is a module it will be inplace\n        #   then it should be moved back to the\n        #   original device\n        x = move_to_device(x, device)\n        model_copy = copy.deepcopy(x)\n        x = move_to_device(x, current_device)\n    else:\n        model_copy = copy.deepcopy(x)\n    return model_copy\n</code></pre>"},{"location":"api/utils/#dataset-utilities","title":"Dataset Utilities","text":""},{"location":"api/utils/#tklearn.utils.datasets","title":"<code>datasets</code>","text":""},{"location":"api/utils/#hashing","title":"Hashing","text":""},{"location":"api/utils/#tklearn.utils.hashing","title":"<code>hashing</code>","text":""},{"location":"api/utils/#index","title":"Index","text":""},{"location":"api/utils/#tklearn.utils.index","title":"<code>index</code>","text":""},{"location":"api/utils/#lexrank","title":"LexRank","text":""},{"location":"api/utils/#tklearn.utils.lexrank","title":"<code>lexrank</code>","text":"<p>LexRank implementation Source: https://github.com/crabcamp/lexrank/tree/dev</p>"},{"location":"api/utils/#subword-utilities","title":"Subword Utilities","text":""},{"location":"api/utils/#tklearn.utils.subword","title":"<code>subword</code>","text":""},{"location":"api/utils/#target-utilities","title":"Target Utilities","text":""},{"location":"api/utils/#tklearn.utils.targets","title":"<code>targets</code>","text":""},{"location":"api/utils/#trie","title":"Trie","text":""},{"location":"api/utils/#tklearn.utils.trie","title":"<code>trie</code>","text":""},{"location":"guides/feature-selection/","title":"Feature Selection Methods","text":"<p>The <code>KBertTokenizer</code> class uses feature selection methods to score and rank predicate-object pairs (triples) extracted from text. This page describes the three available methods and their advantages and disadvantages.</p>"},{"location":"guides/feature-selection/#overview","title":"Overview","text":"<p>When augmenting text with knowledge triples, not all triples are equally useful for classification. Feature selection methods help identify which triples are most relevant by scoring them based on their relationship with class labels.</p>"},{"location":"guides/feature-selection/#methods","title":"Methods","text":""},{"location":"guides/feature-selection/#chi-square-chi2_score","title":"Chi-Square (chi2_score)","text":"<p>Chi-Square measures the statistical dependence between predicate-object pairs and class labels using the chi-squared test.</p> <p>Advantages</p> <ul> <li>Fast computation: Chi-square is computationally efficient, making it suitable for large datasets.</li> <li>Interpretable: The scores have a clear statistical interpretation based on the chi-squared distribution.</li> <li>Works well with term counts: Naturally suited for text classification where features are word or term frequencies.</li> <li>No assumptions about feature distribution: Does not require features to follow a specific distribution.</li> </ul> <p>Disadvantages</p> <ul> <li>Assumes feature independence: Does not account for correlations between features, which may lead to selecting redundant features.</li> <li>Requires non-negative values: Only works with non-negative feature values (e.g., term frequencies or counts).</li> <li>Sensitive to sample size: Results can be unreliable with small sample sizes or sparse data.</li> <li>Linear relationships only: Captures only linear dependencies between features and class labels.</li> </ul>"},{"location":"guides/feature-selection/#mutual-information-mutual_info","title":"Mutual Information (mutual_info)","text":"<p>Mutual Information quantifies the amount of information a feature provides about the class label, based on information theory.</p> <p>Advantages</p> <ul> <li>Captures non-linear dependencies: Unlike chi-square, mutual information can detect both linear and non-linear relationships between features and class labels.</li> <li>Information-theoretic foundation: Grounded in well-established information theory principles.</li> <li>Feature independence not required: Can identify informative features regardless of their correlations with other features.</li> <li>Scale-invariant: Not affected by linear transformations of the feature values.</li> </ul> <p>Disadvantages</p> <ul> <li>Computationally more expensive: Requires density estimation, which can be slower than chi-square, especially for large datasets.</li> <li>Sensitive to binning/discretization: For continuous features, the results depend on how features are discretized.</li> <li>Estimation challenges: Accurate estimation requires sufficient samples; may be unreliable with sparse data.</li> <li>No built-in redundancy handling: Does not inherently account for redundancy among selected features.</li> </ul>"},{"location":"guides/feature-selection/#svm-coefficients-svm_score","title":"SVM Coefficients (svm_score)","text":"<p>SVM Coefficients uses the absolute values of LinearSVC (linear Support Vector Machine) coefficients as feature importance scores.</p> <p>Advantages</p> <ul> <li>Considers feature interactions: Through margin optimization, SVM implicitly considers how features work together for classification.</li> <li>Direct classification relevance: Provides feature importance directly tied to classification performance.</li> <li>Robust to outliers: SVMs are generally robust to outliers due to the margin-based optimization.</li> <li>Effective in high-dimensional spaces: Works well when the number of features is large relative to the number of samples.</li> </ul> <p>Disadvantages</p> <ul> <li>Requires training a model: More computationally expensive as it requires training a full SVM classifier.</li> <li>May not generalize well: The importance scores are specific to the linear SVM model and may not reflect importance for other classifiers.</li> <li>Sensitive to hyperparameters: Results depend on regularization parameters (e.g., C parameter).</li> <li>Assumes linear separability: Uses a linear kernel, which may not capture complex non-linear relationships.</li> </ul>"},{"location":"guides/feature-selection/#comparison-table","title":"Comparison Table","text":"Method Computation Speed Non-linear Dependencies Feature Interactions Model-Free Chi-Square Fast No No Yes Mutual Information Moderate Yes No Yes SVM Coefficients Slow No Partial No"},{"location":"guides/feature-selection/#recommendations","title":"Recommendations","text":"<ul> <li>Use Chi-Square when you need fast feature selection and your data has clear linear relationships between features and class labels.</li> <li>Use Mutual Information when you suspect non-linear relationships between features and class labels, and computational cost is not a primary concern.</li> <li>Use SVM Coefficients when you want feature importance that directly reflects classification relevance and are willing to invest in training time.</li> </ul>"}]}